MemoryVault - ProjectTillNow
Date: 2026-02-28

==============================
What Is Completed
==============================
1. Monorepo scaffold created at `memoryvault/` with `client`, `server`, `docs`, and root progress log.
2. Frontend initialized using Next.js App Router + TypeScript + TailwindCSS architecture.
3. D3.js family tree renderer implemented with dynamic hierarchical rendering and zoom/pan.
4. Infinite scroll member loading implemented in tree detail page using IntersectionObserver + paginated backend API.
5. Backend initialized with Express + MongoDB (Mongoose) setup.
6. JWT authentication complete with register/login endpoints and protected middleware.
7. Core Mongoose models created: `User`, `FamilyTree`, `Member`.
8. Multer-based image upload integrated for member profile images.
9. API docs and setup docs added under `docs/`.

==============================
Folder Structure
==============================
memoryvault/
|- client/
|  |- app/
|  |  |- login/page.tsx
|  |  |- register/page.tsx
|  |  |- tree/[id]/page.tsx
|  |  |- globals.css
|  |  |- layout.tsx
|  |  |- page.tsx
|  |- components/FamilyTreeChart.tsx
|  |- components/FocusTreeCanvas.tsx
|  |- lib/
|  |  |- api.ts
|  |  |- auth.ts
|  |- types/index.ts
|  |- .env.local.example
|  |- .eslintrc.json
|  |- .gitignore
|  |- next.config.mjs
|  |- package.json
|  |- postcss.config.js
|  |- tailwind.config.ts
|  |- tsconfig.json
|- server/
|  |- src/
|  |  |- config/
|  |  |  |- db.js
|  |  |  |- multer.js
|  |  |- controllers/
|  |  |  |- authController.js
|  |  |  |- memberController.js
|  |  |  |- treeController.js
|  |  |- middleware/
|  |  |  |- authMiddleware.js
|  |  |  |- errorMiddleware.js
|  |  |  |- treeAccessMiddleware.js
|  |  |- models/
|  |  |  |- FamilyTree.js
|  |  |  |- Member.js
|  |  |  |- User.js
|  |  |- routes/
|  |  |  |- authRoutes.js
|  |  |  |- memberRoutes.js
|  |  |  |- treeRoutes.js
|  |  |- utils/
|  |  |  |- generateToken.js
|  |  |  |- validateRequest.js
|  |  |- app.js
|  |  |- server.js
|  |- uploads/.gitkeep
|  |- .env.example
|  |- .gitignore
|  |- package.json
|- docs/
|  |- API.md
|  |- SETUP.md
|- .gitignore
|- ProjectTillNow.txt

==============================
Installed Dependencies
==============================
Client (`client/package.json`)
- next
- react
- react-dom
- d3
- typescript
- tailwindcss
- postcss
- autoprefixer
- eslint
- eslint-config-next
- @types/node
- @types/react
- @types/react-dom
- @types/d3

Server (`server/package.json`)
- express
- mongoose
- dotenv
- cors
- morgan
- bcryptjs
- jsonwebtoken
- express-validator
- multer
- helmet
- express-rate-limit
- xss
- nodemon

==============================
Environment Variables Needed
==============================
Server (`server/.env`)
- NODE_ENV=development
- PORT=5000
- MONGO_URI=mongodb://127.0.0.1:27017/memoryvault
- JWT_SECRET=replace_with_a_long_secure_secret
- JWT_EXPIRES_IN=7d
- JWT_ISSUER=memoryvault-api
- JWT_AUDIENCE=memoryvault-client
- FRONTEND_URL=http://localhost:3000
- MAX_UPLOAD_SIZE_MB=5
- RATE_LIMIT_WINDOW_MS=900000
- RATE_LIMIT_MAX=600
- AUTH_RATE_LIMIT_MAX=25

Client (`client/.env.local`)
- NEXT_PUBLIC_API_URL=http://localhost:5000/api
- NEXT_PUBLIC_UPLOADS_URL=http://localhost:5000

==============================
Progress Log (Appended by Major Update)
==============================
[2026-02-28 | Major Update 1]
- Created scalable monorepo folder structure and baseline docs.

[2026-02-28 | Major Update 2]
- Initialized Next.js frontend with App Router, TypeScript, TailwindCSS, auth screens, dashboard, D3 tree chart, and infinite-scroll member loading page.

[2026-02-28 | Major Update 3]
- Initialized Express backend, MongoDB connection module, JWT auth flow (register/login), protected middleware, and robust error handling.

[2026-02-28 | Major Update 4]
- Implemented FamilyTree and Member models, tree/member routes/controllers, Multer image upload, paginated member APIs, and endpoint documentation.

[2026-02-28 | Major Update 5]
- Database updates:
  - `Member` schema refactored to relation-centric structure with `treeId`, `fatherId`, `motherId`, `spouses[]`, `children[]`, `siblings[]`, `name`, `note`, and profile image support.
  - Added relation indexes for tree-parent lookups and maintained derived relation integrity via backend rebuild logic.
- APIs created/updated:
  - `POST /api/trees/:treeId/members` -> Add Member with relation binding support (`relationType`, `relatedMemberId`) and image upload.
  - `PUT /api/trees/:treeId/members/:memberId` -> Update Member details (+ optional parent/spouse updates, image replace).
  - `DELETE /api/trees/:treeId/members/:memberId?subtree=true|false` -> Delete single member or full descendant subtree.
  - `GET /api/trees/:treeId/members/:memberId/relations` -> Get member with resolved father/mother/spouses/children/siblings.
  - `GET /api/trees/:treeId/members/:memberId` -> Get member with relations.
  - `GET /api/trees/:treeId` enhanced with `memberCount` and `initialFocusMember` to bootstrap focus engine.
- Tree rendering logic:
  - Added `FocusTreeCanvas` D3 component with center-focused graph composition and relation grouping around the active member.
  - Added smooth D3 transitions for node/link enter/update/exit and animated re-centering on focus changes.
  - Added zoom controls (`+/-`), Ctrl+wheel zoom, and drag pan support.
- Focus engine logic:
  - Implemented directional navigation mapping: scroll up (parent), down (child), left (sibling), right (spouse).
  - Added keyboard directional fallback via arrow keys.
  - On each focus change, frontend fetches fresh related nodes from backend and re-renders around the new focus.
- UI modals and interactions:
  - Added `Add Member` modal (Name, Profile Image, Relation Type, Optional Note).
  - Node click opens detailed modal with full member data, relation summary, member update form, and subtree delete toggle.

[2026-02-28 | Major Update 6]
- Tree privacy system implemented:
  - Added `privacy` (`public` / `private`) and `passwordHash` fields to `FamilyTree`.
  - Private tree access now requires password validation through middleware for non-owner/non-admin users.
- Access middleware added:
  - `attachTreeContext` to load and validate tree context.
  - `requireTreeReadAccess` to permit public trees or validate private password.
  - `requireTreeWriteAccess` to enforce owner/admin write operations.
- Role system added:
  - Added `role` field in `User` model with values `admin` and `user`.
  - Auth response now includes role in user payload.
  - Admin role can manage any tree (read/write), user role is restricted by ownership + privacy.
- Dashboard upgraded:
  - Create Tree now supports privacy selection and private-tree password.
  - List Trees enhanced with privacy badges, owner info, member count, and edit capability indicator.
  - Delete Tree action added (also removes all members in that tree).
  - Edit Tree Settings modal added for name, description, privacy mode, and password updates.
- Tree API updates completed:
  - `PUT /api/trees/:treeId` for tree settings updates.
  - `DELETE /api/trees/:treeId` for tree deletion.
  - `GET /api/trees/:treeId` now validated via tree access middleware and returns edit capability.
- Private tree frontend access flow:
  - Added password unlock modal on tree page when private access is required.
  - Tree password is persisted per tree in session storage and sent as `x-tree-password` header for related node/member fetches.

[2026-02-28 | Major Update 7]
- Large-tree optimization (1000+ nodes) completed:
  - Added lazy relation loading around focus node with backend query controls (`childrenPage`, `childrenLimit`, `spouseLimit`, `siblingLimit`) on:
    - `GET /api/trees/:treeId/members/:memberId/relations`
    - `GET /api/trees/:treeId/members/:memberId`
  - Added children pagination metadata in relation response (`relationMeta.children`) and frontend "Load More Children" flow.
- API/data engine updates:
  - Refactored `buildMemberWithRelations` to return paginated children and bounded spouse/sibling batches.
  - Optimized member deletion subtree traversal using indexed parent lookups instead of full in-memory tree scans.
  - Reworked derived relation rebuild to linear-time map/group based processing (children/siblings/spouse reciprocity) for faster mutation handling.
- Database/indexing updates:
  - Expanded `Member` indexes for high-volume relation queries:
    - `{ treeId, fatherId, createdAt }`
    - `{ treeId, motherId, createdAt }`
    - `{ treeId, spouses }`, `{ treeId, siblings }`, `{ treeId, children }`
    - `{ treeId, name }`, `{ treeId, updatedAt }`, `{ treeId, _id }`
- Data integrity validation updates:
  - Added schema-level relation normalization/deduplication hooks for spouses/children/siblings.
  - Enforced parent validity rules: no self-parent, no duplicate father/mother id, parent ids must exist in same tree.
  - Added parent cycle detection to block invalid ancestry updates.
  - Hardened spouse sync to prevent duplicate or one-sided spouse relations.
- D3 rendering performance updates:
  - Added render caps for visible spouses/siblings/children per frame to avoid large DOM spikes.
  - Added adaptive transition strategy (reduced/disabled animation at high visible-node counts) for smoother interactions.
  - Preserved directional focus navigation while rendering incrementally loaded children.

[2026-02-28 | Major Update 8]
- Security hardening completed for production readiness.
- Secure image upload:
  - Hardened Multer configuration to allow only `image/jpeg`, `image/png`, `image/gif`, `image/webp`.
  - Enforced configurable upload size limit via `MAX_UPLOAD_SIZE_MB`.
  - Implemented cryptographically unique filenames for uploads.
  - Upload storage remains under `server/uploads` and served from `/uploads`.
  - Added post-upload magic-byte signature validation middleware to block MIME spoofing.
- NoSQL injection protection:
  - Added global request sanitization middleware that strips dangerous object keys (`$`, `.`, null-byte patterns) from `req.body`, `req.query`, and `req.params`.
- XSS protection:
  - Added input sanitization middleware using strict HTML/script stripping for non-sensitive fields.
  - Multipart form payloads are sanitized after Multer processing before member controllers execute.
  - Added Helmet security headers at app level.
- CSRF protection:
  - Added state-changing request origin/referer guard middleware for unsafe HTTP methods.
  - Added per-session CSRF token issuance on register/login and enforced `x-csrf-token` validation for authenticated unsafe routes.
  - Frontend now stores CSRF token and automatically sends it on unsafe API methods.
- Duplicate JWT misuse protection:
  - JWT payload now includes `jti` (token id) and `tv` (token version).
  - Added `tokenVersion` and `activeTokenId` checks in auth middleware.
  - Every login/register rotates session identifiers and invalidates older JWTs.
  - Added issuer/audience verification (`JWT_ISSUER`, `JWT_AUDIENCE`) in token sign/verify paths.
- Rate limiting:
  - Added global API rate limiting and stricter auth rate limiting using `express-rate-limit`.
- Error handling:
  - Extended error middleware for Multer-specific errors (size/invalid upload failures) with safe client messages.
- Environment/config additions:
  - `JWT_ISSUER`, `JWT_AUDIENCE`
  - `MAX_UPLOAD_SIZE_MB`
  - `RATE_LIMIT_WINDOW_MS`, `RATE_LIMIT_MAX`, `AUTH_RATE_LIMIT_MAX`
- Dependency additions (server):
  - `helmet`
  - `express-rate-limit`
  - `xss`

[2026-02-28 | Session Close Checklist]
- Completed modules:
  - Repository workflow rule for mandatory end-of-session project log updates.
- File changes:
  - Added `AGENTS.md` (root) with required finishing checklist.
- Database schema state:
  - No schema changes in this step (unchanged from Major Update 8).
- Pending tasks:
  - Continue implementing requested product features and maintain this checklist at every session end.

[2026-02-28 | Major Update 9]
- Complete Tree Engine + Relation Logic implementation completed (continued from existing state; no setup regeneration).
- Backend relation engine APIs added:
  - `PATCH /api/trees/:treeId/members/:memberId/relations`
    - Supports `connect` / `disconnect` for `father`, `mother`, `child`, `spouse`, `sibling`.
    - Supports `parentRole` (`father` | `mother` | `auto`) for child relation logic.
  - `GET /api/trees/:treeId/members/:memberId/graph?depth=&limit=`
    - Returns graph neighborhood (`nodes` + typed `links`) for focused traversal.
- Relation logic upgrades:
  - Added centralized mutation logic to enforce reciprocal spouse/sibling links.
  - Added robust parent assignment/removal rules for child relations.
  - Preserved cycle prevention and parent integrity checks during relation mutations.
  - Extended derived relation rebuild to merge explicit sibling links + parent-derived siblings.
- Member listing/search upgrades:
  - Added optional member name search (`search`) to paginated members API for relation targeting.
- Frontend tree engine upgrades:
  - Added relation management UI inside Member Details modal:
    - Action selector (`connect` / `disconnect`)
    - Relation selector (`father`, `mother`, `child`, `spouse`, `sibling`)
    - Parent role selector for child links
    - Search + target member selector
  - Integrated relation mutation calls and automatic focus/detail refresh behavior.
- Client API/types upgraded:
  - Added typed APIs for relation mutation, member graph fetch, and member search.
  - Added relation mutation and graph response TypeScript contracts.
- Documentation updated:
  - `docs/API.md` now includes relation mutation endpoint, graph endpoint, relation pagination query params, search support, and CSRF in auth response.

[2026-02-28 | Session Close Checklist]
- Completed modules:
  - Complete relation mutation engine (connect/disconnect) for all core family relations.
  - Tree graph neighborhood API for engine traversal.
  - Frontend relation-management controls in tree detail modal.
  - API/type/docs alignment for new relation engine behavior.
- File changes:
  - `server/src/controllers/memberController.js`
  - `server/src/routes/memberRoutes.js`
  - `client/app/tree/[id]/page.tsx`
  - `client/lib/api.ts`
  - `client/types/index.ts`
  - `docs/API.md`
- Database schema state:
  - No new schema fields added in this update.
  - Existing `Member` relation fields are actively used by the new mutation engine.
  - Existing relation integrity/cycle rules remain enforced.
- Pending tasks:
  - Optional: add dedicated UI graph-mode toggle to consume `/graph` endpoint directly.
  - Optional: add automated integration tests for relation mutation edge cases (cycle, two-parent constraints, disconnect behavior).
  - Optional: replace `<img>` with `next/image` in tree detail modal to remove current lint warning.
- Runtime status:
  - Server syntax checks passed for updated files.
  - Client production build passed successfully (only existing non-blocking Next.js `<img>` warning remains).
- Post-validation cleanup:
  - Removed redundant relation type duplication in `client/types/index.ts` for graph link relation typing.

[2026-02-28 | Major Update 10]
- PHASE: CORE FAMILY TREE ENGINE backend compatibility completed without regenerating existing setup.
- Member schema updates:
  - Added `metadata` field (`Mixed`, default `{}`) to `Member` model.
  - Added explicit single-field indexes for `treeId`, `fatherId`, and `motherId` (in addition to existing compound relation indexes).
- Relationship engine logic status:
  - Existing relation engine already enforces child-parent assignment, sibling parent-sharing behavior, spouse bi-directional linking, duplicate-prevention, self-link prevention, and cycle validation.
  - No duplicate relation logic introduced; new endpoints reuse existing member controller logic.
- New backend APIs added (exact phase endpoints):
  - `POST /api/member/add` -> routes into existing create-member relation engine.
  - `PUT /api/member/update/:id` -> routes into existing update-member relation engine.
  - `DELETE /api/member/delete/:id` -> routes into existing delete-member relation engine.
  - `GET /api/member/:id` -> returns member with relations from existing engine.
  - `GET /api/tree/:treeId/focus/:memberId` -> returns `{ center, parents, spouses, siblings, children }`.
- Middleware/context additions for compatibility endpoints:
  - Added `memberContextMiddleware` to resolve `treeId` from body/member id and preserve tree access control middleware flow.
- API documentation updated:
  - `docs/API.md` now includes the phase compatibility endpoints and focus response shape.

[2026-02-28 | Session Close Checklist]
- Completed modules:
  - Core phase-compatible member APIs (`/api/member/*`) using existing production relation engine.
  - Focus endpoint (`/api/tree/:treeId/focus/:memberId`) with required response contract.
  - Member schema `metadata` support + explicit core indexes.
  - Context middleware to map compatibility routes to tree-aware access controls.
- File changes:
  - `server/src/models/Member.js`
  - `server/src/middleware/memberContextMiddleware.js`
  - `server/src/controllers/focusController.js`
  - `server/src/routes/coreMemberRoutes.js`
  - `server/src/routes/coreFocusRoutes.js`
  - `server/src/app.js`
  - `docs/API.md`
- Database schema state:
  - `Member` schema includes:
    - `treeId`, `name`, `profileImage`, `fatherId`, `motherId`, `spouses[]`, `children[]`, `siblings[]`, `gender`, `birthDate`, `deathDate`, `note`, `metadata`.
  - Existing additional fields (`createdBy`, `bio`) remain backward-compatible.
  - Core indexes present for `treeId`, `fatherId`, `motherId` plus existing relation-performance indexes.
- Pending tasks:
  - Optional: migrate frontend to directly consume `/api/member/*` compatibility endpoints if needed by external clients.
  - Optional: add automated endpoint tests for compatibility APIs + focus payload contract.
  - Optional: replace existing tree-page `<img>` with `next/image` to remove non-blocking lint warning.
- Runtime status:
  - Server syntax checks passed for all new/updated backend files.
  - Client production build passed successfully (only pre-existing `<img>` lint warning remains).

[2026-02-28 | Major Update 10 - Addendum]
- Expanded phase backend alignment:
  - Added compatibility middleware-driven routing so `/api/member/*` endpoints reuse existing production relation controller logic (no duplicate business engine code).
  - Added support for optional `gender`, `birthDate`, `deathDate`, and `metadata` inputs in member create/update engine.
  - Added dedicated focus controller for required response contract `{ center, parents, spouses, siblings, children }`.
- Documentation refinement:
  - Added compatibility endpoint field coverage details in `docs/API.md`.

[2026-02-28 | Session Close Checklist]
- Completed modules:
  - Phase compatibility member APIs (`/api/member/add`, `/api/member/update/:id`, `/api/member/delete/:id`, `/api/member/:id`).
  - Required focus endpoint (`/api/tree/:treeId/focus/:memberId`) with exact contract sections.
  - Member schema enhancement (`metadata`) and explicit core indexes (`treeId`, `fatherId`, `motherId`).
  - Create/update support for schema-aligned demographic/metadata fields.
- File changes:
  - `server/src/models/Member.js`
  - `server/src/middleware/memberContextMiddleware.js`
  - `server/src/controllers/focusController.js`
  - `server/src/controllers/memberController.js`
  - `server/src/routes/coreMemberRoutes.js`
  - `server/src/routes/coreFocusRoutes.js`
  - `server/src/app.js`
  - `docs/API.md`
  - `ProjectTillNow.txt`
- Database schema state:
  - `Member` includes required phase fields (`treeId`, `name`, `profileImage`, `fatherId`, `motherId`, `spouses[]`, `children[]`, `siblings[]`, `gender`, `birthDate`, `deathDate`, `note`, `metadata`).
  - Existing backward-compatible fields remain (`createdBy`, `bio`).
  - Explicit indexes available for `treeId`, `fatherId`, `motherId` along with relation-performance indexes.
- Pending tasks:
  - Add automated tests for compatibility endpoints and focus payload contract.
  - Optionally expose compatibility endpoints in frontend client wrappers (if external consumers will use them directly).
  - Optionally migrate remaining `<img>` to `next/image` in frontend for lint cleanup.
- Runtime status:
  - Node syntax checks passed for all new/updated backend files.
  - Client production build passes (only existing non-blocking `<img>` lint warning).

[2026-02-28 | Major Update 11]
- Frontend complete tree canvas phase implemented on tree detail page with production wiring.
- TreeCanvas integration:
  - `TreeCanvas` is now used as the rendering component for focused tree interaction.
  - Focus remains center-anchored with D3 transition-based updates.
- Focus engine navigation implemented and validated:
  - Arrow key navigation wired: `ArrowUp -> parent`, `ArrowDown -> child`, `ArrowLeft -> sibling`, `ArrowRight -> spouse`.
  - Existing wheel directional navigation remains active (up/down/left/right semantics) with smooth focus transitions.
  - Drag-to-pan and zoom controls (`+`, `-`, Ctrl+wheel) remain active and stable.
- Focus change backend flow updated:
  - Focus changes now call `GET /api/tree/:treeId/focus/:memberId` through `getTreeFocus(...)`.
  - Response is mapped into canvas relation bundle and tree re-renders around the new center member.
- Add Member modal updated to requested fields:
  - Name
  - Relation Type dropdown (Father, Mother, Spouse, Son, Daughter, Brother, Sister)
  - Gender
  - Optional Note
  - Profile Image Upload
  - For initial root creation with no selected focus node, relation is ignored and member is created as root (UI hint shown).
- Node click detail flow enhanced:
  - Node click opens Member Detail modal.
  - Modal now displays full member info set including ID, gender, birth/death dates, created/updated dates, profile image path, note, metadata (if present), and relation summaries.
- Stability fixes from previous partial transition:
  - Removed stale `loadMoreChildren()` button reference after focus API migration.
  - Replaced stale `FocusTreeCanvas` JSX usage with `TreeCanvas` usage.
  - Fixed add-form typing mismatch for relation selection and payload mapping.

[2026-02-28 | Session Close Checklist]
- Completed modules:
  - Tree canvas integration + centered focus navigation UI.
  - Arrow-key based relation traversal and focus refresh flow.
  - Focus API-based re-render pipeline (`/api/tree/:treeId/focus/:memberId`).
  - Add Member modal field completion and relation mapping for requested options.
  - Member detail modal enrichment for full info display on node click.
- File changes:
  - `client/app/tree/[id]/page.tsx`
  - `ProjectTillNow.txt`
- Database schema state:
  - No schema changes in this update.
  - Existing schemas remain active:
    - `Member`: `treeId`, `name`, `profileImage`, `fatherId`, `motherId`, `spouses[]`, `children[]`, `siblings[]`, `gender`, `birthDate`, `deathDate`, `note`, `metadata` (+ existing compatibility fields).
    - `FamilyTree`: privacy/password-protection fields unchanged.
    - `User`: role system (`admin`/`user`) unchanged.
- Pending tasks:
  - Optional: migrate detail modal `<img>` to `next/image` to clear current Next.js lint warning.
  - Optional: add automated frontend interaction tests for keyboard navigation and focus API refresh behavior.
- Runtime status:
  - Client build passes successfully after this update.
  - Existing non-blocking lint warning persists for `<img>` in member detail modal.

[2026-02-28 | Major Update 12]
Schema changes
- No new schema migrations in this step.
- Existing active schema remains:
  - `Member`: `treeId`, `name`, `profileImage`, `fatherId`, `motherId`, `spouses[]`, `children[]`, `siblings[]`, `gender`, `birthDate`, `deathDate`, `note`, `metadata` (+ compatibility fields).
  - `FamilyTree`: privacy/password-protection fields unchanged.
  - `User`: role system unchanged (`admin` / `user`).

APIs created
- No new endpoints added in this step.
- Existing tree engine flow validated to use:
  - `GET /api/tree/:treeId/focus/:memberId` (focus fetch on navigation)
  - Existing member relation/detail APIs for modal actions and updates.

Tree rendering logic
- `TreeCanvas` remains the active D3 renderer for the tree page.
- Center-focused rendering with animated node/link transitions is active.
- Drag-to-pan, Ctrl+wheel zoom, and +/- zoom controls are active.
- Canvas re-renders with fresh relation bundle after focus updates.

Focus engine logic
- Directional navigation remains active and validated:
  - `ArrowUp` -> parent
  - `ArrowDown` -> child
  - `ArrowLeft` -> sibling
  - `ArrowRight` -> spouse
- Wheel directional navigation remains active with the same mapping.
- On every focus change, client calls focus API and re-renders around new center node.

Pending tasks
- Optional: replace member detail modal `<img>` with `next/image` to clear non-blocking lint warning.
- Optional: add automated tests for keyboard focus navigation and focus API refresh behavior.
- Optional: add integration tests for add-member relation mapping (Father/Mother/Spouse/Son/Daughter/Brother/Sister).

[2026-02-28 | Session Close Checklist]
- Completed modules:
  - Structured project-log update with required schema/API/render/focus/pending sections.
  - Full runtime validation for current implementation state.
- File changes:
  - `ProjectTillNow.txt`
- Database schema state:
  - Unchanged in this step; existing production schema remains valid.
- Pending tasks:
  - See `Pending tasks` section above.
- Runtime status:
  - Client build: passed (`npm run build`).
  - Server syntax validation: passed (`node --check` across `server/src/**/*.js`).
  - No existing working code removed in this step.

[2026-02-28 | Major Update 13]
- Continued from pending/backlog compatibility tasks without regenerating completed modules.
- Implemented backend compatibility tree/admin surface while reusing existing controllers/middleware to avoid duplicate business logic.

Schema changes
- `FamilyTree` schema updated with compatibility fields:
  - `isPrivate` (Boolean, default `false`)
  - `accessPassword` (String, hashed, optional)
- Added schema sync hook so both legacy and compatibility fields stay consistent:
  - `privacy` <-> `isPrivate`
  - `passwordHash` <-> `accessPassword`
- Tree response sanitizer now removes both `passwordHash` and `accessPassword` from API payloads.

APIs created
- Tree Management compatibility APIs (`/api/tree/*`):
  - `POST /api/tree/create`
  - `GET /api/tree/my-trees`
  - `PUT /api/tree/update/:id`
  - `DELETE /api/tree/delete/:id`
- Admin APIs (`/api/admin/*`):
  - `GET /api/admin/all-trees`
  - `DELETE /api/admin/delete-tree/:id`
  - `GET /api/admin/all-users`

Tree privacy system updates
- Added `validateTreeAccess` middleware.
- Behavior:
  - Public tree -> access allowed.
  - Private tree -> requires valid password unless owner/admin.
  - On successful private password verification, middleware issues short-lived `x-tree-access-token` for subsequent access.
- Existing read-access middleware now reuses `validateTreeAccess` logic.

Access and role rules
- JWT protection enforced for compatibility tree/admin routes.
- Compatibility tree update/delete endpoints are owner-only (`requireTreeOwnerAccess`).
- Admin endpoints protected via `protect + authorizeRoles("admin")`.
- Tree deletion remains cascading (tree + all members in that tree).

Tree rendering logic
- No rendering engine changes in this update.
- Existing `TreeCanvas` centered focus rendering, animation, zoom/pan, and D3 transitions remain unchanged and working.

Focus engine logic
- No navigation mapping changes in this update.
- Existing focus flow remains active:
  - Arrow keys: parent/child/sibling/spouse
  - Focus API fetch + re-render around center node
- Focus route now uses `validateTreeAccess` middleware for private-tree gate consistency.

Documentation updates
- `docs/API.md` updated with:
  - compatibility `/api/tree/*` endpoints
  - admin `/api/admin/*` endpoints
  - private-tree short-lived access token behavior (`x-tree-access-token`)

[2026-02-28 | Session Close Checklist]
- Completed modules:
  - Compatibility tree management API layer (`/api/tree/create`, `/my-trees`, `/update/:id`, `/delete/:id`).
  - Tree privacy middleware alias (`validateTreeAccess`) with short-lived tree access token issuance.
  - Owner-only middleware for compatibility tree mutations.
  - Admin management endpoints (`all-trees`, `delete-tree`, `all-users`) with admin-role protection.
- File changes:
  - `server/src/models/FamilyTree.js`
  - `server/src/controllers/treeController.js`
  - `server/src/controllers/adminController.js`
  - `server/src/middleware/treeAccessMiddleware.js`
  - `server/src/routes/coreTreeRoutes.js`
  - `server/src/routes/adminRoutes.js`
  - `server/src/routes/coreFocusRoutes.js`
  - `server/src/routes/treeRoutes.js`
  - `server/src/app.js`
  - `docs/API.md`
  - `ProjectTillNow.txt`
- Database schema state:
  - `FamilyTree` now supports both privacy representations:
    - legacy: `privacy`, `passwordHash`
    - compatibility: `isPrivate`, `accessPassword` (hashed)
  - Sync hook keeps both sets aligned to prevent drift.
  - `User.role` remains `admin | user`.
- Pending tasks:
  - Optional: add automated integration tests for new `/api/tree/*` compatibility routes and `/api/admin/*` role guards.
  - Optional: expose new compatibility/admin endpoints in frontend API client wrappers if UI should consume them directly.
  - Optional: migrate remaining tree detail `<img>` to `next/image` to clear non-blocking lint warning.
- Runtime status:
  - Server syntax validation passed (`node --check` on all `server/src/**/*.js`).
  - Client production build passed (`npm run build`), with existing non-blocking Next.js `<img>` lint warning.
  - Existing working modules were preserved; no regeneration/removal performed.

[2026-02-28 | Major Update 14]
- Continued from pending tasks only; existing modules were reused and not regenerated.
- Frontend dashboard and tree UX enhancements implemented with compatibility access-token flow.

Frontend implementation
1) Dashboard page enhancements
- Dashboard now clearly provides required management sections/actions:
  - Create New Tree
  - List My Trees
  - Edit Tree (modal)
  - Delete Tree
  - Toggle Public / Private (per-tree quick action)
  - Set Password when switching/creating private tree
- Added `My Trees` anchor section id (`#my-trees`) for top-nav navigation.
- Quick privacy toggle added with password prompt when toggling to private.

2) Private tree prompt + temporary access token
- Existing private-tree password prompt retained before loading protected trees.
- Added short-lived tree access token support in client API layer:
  - request sends `x-tree-access-token` when available
  - request stores refreshed `x-tree-access-token` from response headers in session storage
- Tree page now clears both stored tree password and tree access token on private-access errors and on tree-id context switch.

3) Top Navigation Bar
- Added shared authenticated top navigation in layout with:
  - Dashboard
  - My Trees
  - Logout
- Nav hides automatically on unauthenticated and auth pages (`/login`, `/register`).
- Logout clears JWT/user/CSRF and session-scoped tree password/access-token cache.

4) Tree UI improvements
- Replaced modal `<img>` usage with Next.js `<Image />` component.
- Removed previous Next.js lint warning for `no-img-element`.
- Improved D3 node visual styling:
  - updated color palette
  - node drop-shadow filter
  - hover effects (raise, ring glow, radius expansion, label emphasis)
- Added focus-loading skeleton overlay while related nodes are being fetched.

Runtime validation
- Client production build passed (`npm run build`) with no lint warnings.
- Server syntax validation passed (`node --check` for all `server/src/**/*.js`).

[2026-02-28 | Session Close Checklist]
- Completed modules:
  - Shared top navigation bar with Dashboard/My Trees/Logout.
  - Dashboard tree-management UX completion (create/list/edit/delete/privacy toggle/password handling).
  - Private-tree temporary access-token client flow (`x-tree-access-token`) added.
  - Tree detail modal image migrated to Next.js `Image`.
  - D3 node styling + hover enhancements.
  - Focus-loading skeleton overlay during focus API fetches.
- File changes:
  - `client/components/TopNav.tsx`
  - `client/app/layout.tsx`
  - `client/app/page.tsx`
  - `client/lib/api.ts`
  - `client/app/tree/[id]/page.tsx`
  - `client/components/FocusTreeCanvas.tsx`
  - `ProjectTillNow.txt`
- Database schema state:
  - No new schema changes in this frontend update.
  - Backend compatibility schema from previous update remains active (`FamilyTree` supports legacy+compat privacy/password fields).
- Pending tasks:
  - Optional: add frontend tests for private-tree password/token refresh flow and dashboard privacy toggle flow.
  - Optional: add e2e coverage for top-nav guarded behavior across auth/non-auth routes.
  - Optional: add visual regression tests for D3 node hover and loading skeleton states.
- Runtime status:
  - Client build: passed.
  - Server syntax check: passed.
  - Existing working system preserved; no module regeneration/removal performed.

[2026-02-28 | Major Update 15]
- Security hardening phase extended without regenerating existing modules.
- Added explicit Mongo sanitization middleware and migrated request-body validation to Joi across backend APIs.

Security hardening updates
1) Helmet
- Already active in `server/src/app.js` and retained as global security-header middleware.

2) Rate limiter
- Already active in `server/src/app.js` for:
  - global `/api` traffic
  - stricter `/api/auth` traffic
- Existing configuration retained and validated.

3) CORS config
- Existing strict allowed-origin list (`FRONTEND_URL`-driven) with credentials support retained and validated.

4) Mongo sanitization
- Added `express-mongo-sanitize` package.
- Added global middleware in `server/src/app.js`:
  - strips/replaces Mongo operator-injection keys via `replaceWith: "_"`.
- Existing request sanitizer remains active for additional deep key sanitization.

5) XSS clean
- Existing request sanitization pipeline using `xss`-based HTML/script stripping retained.
- Applied together with Joi validation and Mongo sanitization for layered defense.

Joi body validation rollout
- Added Joi validation middleware (`validateBody`) with:
  - aggregated field errors
  - `stripUnknown`
  - type conversion
- Added centralized Joi schemas in `server/src/validation/bodySchemas.js`.
- Applied Joi body validation to all body-bearing endpoints:
  - Auth:
    - `POST /api/auth/register`
    - `POST /api/auth/login`
  - Tree:
    - `POST /api/trees`
    - `PUT /api/trees/:treeId`
  - Core Tree compatibility:
    - `POST /api/tree/create`
    - `PUT /api/tree/update/:id`
  - Member:
    - `POST /api/trees/:treeId/members`
    - `PUT /api/trees/:treeId/members/:memberId`
    - `PATCH /api/trees/:treeId/members/:memberId/relations`
  - Core Member compatibility:
    - `POST /api/member/add`
    - `PUT /api/member/update/:id`

Validation behavior notes
- Multipart member routes now validate body after Multer parsing and sanitization.
- Metadata, nullable dates, nullable relation ids, and spouse list formats are Joi-validated.
- Private tree creation requires password through Joi pre-validation as well as controller enforcement.

Runtime validation
- Server syntax validation passed (`node --check` across all `server/src/**/*.js`).
- Server app bootstrap check passed (`require('./src/app')`).
- Client build passed (`npm run build`).

[2026-02-28 | Session Close Checklist]
- Completed modules:
  - Added Mongo sanitization middleware (`express-mongo-sanitize`).
  - Added centralized Joi request-body validation framework.
  - Migrated all body-bearing routes to Joi validation.
  - Preserved existing Helmet, Rate Limiting, CORS, and XSS cleaning stack.
- File changes:
  - `server/package.json`
  - `server/package-lock.json`
  - `server/src/app.js`
  - `server/src/middleware/joiValidationMiddleware.js`
  - `server/src/validation/bodySchemas.js`
  - `server/src/routes/authRoutes.js`
  - `server/src/routes/treeRoutes.js`
  - `server/src/routes/coreTreeRoutes.js`
  - `server/src/routes/memberRoutes.js`
  - `server/src/routes/coreMemberRoutes.js`
  - `ProjectTillNow.txt`
- Database schema state:
  - No new schema fields in this step.
  - Existing schemas remain unchanged from prior updates.
- Pending tasks:
  - Optional: add automated API tests for Joi validation failure payloads per route.
  - Optional: resolve pre-existing duplicate Mongoose index warnings in `Member` model (`treeId`, `fatherId`, `motherId`) to remove bootstrap warnings.
  - Optional: document Joi schemas in `docs/API.md` with invalid-request examples.
- Runtime status:
  - Server syntax: passed.
  - Server bootstrap: passed (with pre-existing duplicate index warnings).
  - Client production build: passed.
  - Existing working modules preserved; no code removal/regeneration performed.

[2026-02-28 | Final Production Readiness Update]
New APIs
- Tree management compatibility APIs:
  - `POST /api/tree/create`
  - `GET /api/tree/my-trees`
  - `PUT /api/tree/update/:id`
  - `DELETE /api/tree/delete/:id`
- Admin APIs:
  - `GET /api/admin/all-trees`
  - `DELETE /api/admin/delete-tree/:id`
  - `GET /api/admin/all-users`
- Existing tree engine APIs preserved and active (including focus, member relation, graph, and compatibility member endpoints).

Dashboard features
- Dashboard now supports:
  - Create New Tree
  - List My Trees
  - Edit Tree settings
  - Delete Tree
  - Toggle Public/Private privacy per tree
  - Set password when creating/switching to private
- Added authenticated top navigation:
  - Dashboard
  - My Trees
  - Logout

Privacy system
- Private tree access requires password before loading protected tree data.
- Added short-lived tree access token support (`x-tree-access-token`) after successful private access validation.
- Client stores tree password/token in session scope and reuses for protected fetches.
- Middleware enforces tree access control for public/private paths while preserving owner/admin bypass rules.

Admin features
- Role model supports `admin` and `user`.
- Admin-only middleware guards admin routes.
- Admin can list all trees, list all users, and delete any tree.

Security additions
- Helmet active for security headers.
- API/global + auth-specific rate limiting active.
- Strict CORS allow-list configuration active.
- Mongo sanitization active (`express-mongo-sanitize`).
- XSS cleaning active in request sanitization middleware.
- Joi body validation added for all body-bearing auth/tree/member routes (including compatibility routes).

Pending tasks
- Add automated integration tests for:
  - Joi validation failure paths
  - Admin-role protected routes
  - Private-tree password/access-token refresh flow
- Optional cleanup: resolve pre-existing duplicate Mongoose index warnings in `Member` model.
- Optional docs expansion: add invalid-request Joi examples in `docs/API.md`.

[2026-02-28 | Session Close Checklist]
- Completed modules:
  - Final production-readiness review and confirmation pass.
  - Required final project log categories appended.
- File changes:
  - `ProjectTillNow.txt`
- Database schema state:
  - No schema changes in this final log step.
  - Existing production schema remains intact.
- Pending tasks:
  - See `Pending tasks` section above.
- Runtime status:
  - Client build: passed (`npm run build`).
  - Server syntax: passed (`node --check` on all `server/src/**/*.js`).
  - Tree engine preserved and not removed/rewritten.
  - No duplicate-code expansion introduced in this final step.

[2026-02-28 | Major Update 16 | PHASE: PRODUCTION DEPLOYMENT + DATA INTEGRITY + LARGE TREE OPTIMIZATION]
Production readiness
- Added transaction-safe write flow to prevent partial writes during high-impact mutations.
- Added backend integrity tooling and admin visibility endpoint.
- Added frontend export + UX safety/feedback flows for production behavior.
- Added deployment artifacts and runbooks for container and managed-platform deployment.

Backend improvements
1) MongoDB transactions
- Added session transaction wrappers for:
  - add member with relation binding (`createMember`)
  - member delete (single + subtree mode) (`deleteMember`)
  - tree delete cascade (`deleteTree`)
- Relation rebuild and root-member fallback updates now run inside the same transaction scope for atomicity.

2) Subtree delete optimization
- Added `collectSubtreeMemberIds` BFS utility using indexed parent lookups (`fatherId`/`motherId`) in batches.
- Delete flow now computes delete set first, then performs relation cleanup + delete + root fallback in one transaction.
- Orphan-reference cleanup remains enforced via relation pulls + derived relation rebuild.

3) MongoDB indexes
- Retained and aligned required relationship indexes for:
  - `treeId`
  - `fatherId`
  - `motherId`
  - `spouses`
  - `children`
  - `siblings`
- Removed duplicate inline path-index flags for `treeId`, `fatherId`, `motherId` and kept explicit schema indexes to reduce duplicate-index warnings.

4) Data integrity checker utility
- Added utility: `server/src/utils/integrityChecker.js`.
- Detects:
  - broken references (missing/self references in parent/relation fields)
  - circular spouse loops (graph cycle detection)
  - duplicate relationship entries in relation arrays
- Added admin endpoint:
  - `GET /api/admin/integrity-check`
  - Optional query: `treeId=<mongo-id>`
- Added CLI script:
  - `npm run integrity:check`

Frontend improvements
1) Tree rendering optimization
- Stabilized tree callbacks with `useCallback` to reduce unnecessary renderer resets.
- Wrapped `FocusTreeCanvas` in `React.memo` for better memoized rendering behavior.
- Added relation-limit support on focus fetch to reduce oversized payload redraw costs.
- Added virtualized children list in member details modal for large child sets.
- Added spinner-enhanced focus loading overlay.

2) Export system
- Added tree export to JSON from tree page (full member pagination fetch).
- Added tree export to PDF (basic generated layout, downloadable `.pdf`).

3) UX improvements
- Added success/error toast stack for key tree actions (add/update/relation/delete/export).
- Added delete confirmation dialog for member/subtree deletion.
- Improved empty-state UI with explicit first-member CTA in focus panel.

DevOps configuration
- Added root `.env.example` covering server + client runtime variables.
- Added Docker assets:
  - `server/Dockerfile`
  - `client/Dockerfile`
  - `docker-compose.yml`
  - `server/.dockerignore`
  - `client/.dockerignore`
- Added production build scripts:
  - `scripts/production-build.sh`
  - `scripts/production-build.ps1`
- Added root `README.md` with:
  - setup and environment instructions
  - development and production run instructions
  - Docker usage
  - deployment guides for VPS (PM2), Render, Vercel (frontend), Railway

Documentation updates
- `docs/API.md` updated for:
  - admin integrity-check endpoint
  - focus endpoint relation metadata and query params

[2026-02-28 | Session Close Checklist]
- Completed modules:
  - Transaction-safe member/tree mutation path for atomic write consistency.
  - Integrity checker utility + admin endpoint + CLI script.
  - Large-tree UI optimization additions (memoized rendering path + child-list virtualization + spinner).
  - JSON/PDF export support on tree page.
  - Docker/compose setup + production build scripts + deployment README.
- File changes:
  - `server/src/controllers/memberController.js`
  - `server/src/controllers/treeController.js`
  - `server/src/controllers/adminController.js`
  - `server/src/controllers/focusController.js`
  - `server/src/routes/adminRoutes.js`
  - `server/src/routes/coreFocusRoutes.js`
  - `server/src/models/Member.js`
  - `server/src/utils/integrityChecker.js`
  - `server/scripts/check-syntax.js`
  - `server/scripts/integrity-check.js`
  - `server/package.json`
  - `server/.env.example`
  - `client/app/tree/[id]/page.tsx`
  - `client/components/FocusTreeCanvas.tsx`
  - `client/lib/api.ts`
  - `client/types/index.ts`
  - `server/Dockerfile`
  - `server/.dockerignore`
  - `client/Dockerfile`
  - `client/.dockerignore`
  - `.env.example`
  - `docker-compose.yml`
  - `scripts/production-build.sh`
  - `scripts/production-build.ps1`
  - `README.md`
  - `docs/API.md`
  - `ProjectTillNow.txt`
- Database schema state:
  - No new member/tree fields added in this update.
  - Index strategy refined and expanded for required relation fields (`treeId`, `fatherId`, `motherId`, `spouses`, `children`, `siblings`).
  - Data integrity is now auditable via utility + admin endpoint.
- Pending future upgrades:
  - Add integration tests for transaction rollback paths (member add/delete/tree delete failure simulations).
  - Add automated tests for integrity checker reports and admin endpoint response contracts.
  - Add e2e tests for export flows (JSON/PDF), toast notifications, and virtualized child-list behavior.
  - Validate Docker image build/run on a host with Docker installed (CLI unavailable in current environment).
- Runtime status:
  - Client build: passed (`cd client && npm run build`).
  - Server syntax/build: passed (`cd server && npm run build`).
  - Docker compose build: could not be executed in this environment because `docker` CLI is not installed.

[2026-02-28 | Major Update 17 | PHASE: SAAS SYSTEM (PLANS + SUBSCRIPTIONS) + MULTI LANGUAGE]
- Continued from pending/new SaaS requirements only; no completed module regeneration performed.
- Implemented production-ready SaaS plan/subscription backend, plan-limit enforcement, pricing/subscription frontend, and multilingual UI foundation (English + Hindi).

Backend implementation
1) Plan system
- Added `Plan` model with fields:
  - `name`
  - `priceMonthly`
  - `priceYearly`
  - `maxMembers`
  - `maxTrees`
  - `features[]`
  - `isActive`
- Added plan APIs (admin-only):
  - `POST /api/admin/plan/create`
  - `GET /api/admin/plan/all`
  - `PUT /api/admin/plan/update/:id`
  - `DELETE /api/admin/plan/delete/:id`
- Added Joi validation schemas for plan create/update payloads.

2) Subscription system
- Added `Subscription` model with fields:
  - `userId`
  - `planId`
  - `startDate`
  - `endDate`
  - `status` (`active | expired | cancelled`)
  - `paymentReference`
  - `billingCycle` (`monthly | yearly`) for lifecycle computation.
- Added subscription APIs (authenticated):
  - `GET /api/subscription/plans` (active plans for pricing page)
  - `POST /api/subscription/subscribe`
  - `GET /api/subscription/my`
  - `POST /api/subscription/cancel`
- Added subscription service utility for:
  - active subscription resolution
  - automatic expiry transition handling
  - owner usage computation
  - unified response summary (`plan + subscription + usage`)

3) Plan limit enforcement
- Enforced tree creation limit (`maxTrees`) in:
  - `POST /api/trees`
  - `POST /api/tree/create`
- Enforced member creation limit (`maxMembers`) in:
  - `POST /api/trees/:treeId/members`
  - `POST /api/member/add`
- Limit violations now return `403` with explicit upgrade/subscription messages.
- Enforcement is centralized in shared service logic to avoid duplicate checks.

Frontend implementation
1) Pricing page
- Added `/pricing` page with:
  - available plan listing
  - monthly/yearly billing toggle
  - subscribe action
  - current subscription summary + cancel action

2) Dashboard SaaS visibility
- Added current plan section and usage panel:
  - trees used / allowed
  - members used / allowed
- Added CTA link to pricing.
- Tree-create button now blocks when tree plan limit is reached.

3) Tree page plan-aware UX
- Added subscription fetch on tree load.
- Disabled add-member actions when member limit is reached:
  - header add-member button
  - empty-state add-first-member button
- Added user-facing limit message when blocked.

Multi-language system
- Added custom i18n provider (Next-compatible client context approach).
- Added language switcher in navbar.
- Added English default locale and Hindi locale.
- Made key tree labels translatable (e.g., Name/Father/Mother/Spouses/Children/Siblings, focus panel labels, add-member labels, export labels, navigation labels).
- Added translated navbar + dashboard + pricing surface labels.

Documentation
- Updated `docs/API.md` with SaaS sections:
  - admin plan endpoints
  - subscription endpoints
  - explicit limit enforcement behavior

Runtime validation
- Server build/syntax: passed (`cd server && npm run build`).
- Client production build: passed (`cd client && npm run build`).
- Existing tree engine behavior preserved (focus/navigation/relation engine logic unchanged).

[2026-02-28 | Session Close Checklist]
- Completed modules:
  - Plan model + admin plan CRUD API layer.
  - Subscription model + subscribe/my/cancel APIs.
  - Plan limit enforcement for tree/member creation with 403 guardrails.
  - Pricing page + dashboard usage visualization + add-member disable at limits.
  - Multi-language system (English/Hindi) with navbar language switcher and translatable tree labels.
- File changes:
  - `server/src/models/Plan.js`
  - `server/src/models/Subscription.js`
  - `server/src/utils/subscriptionService.js`
  - `server/src/controllers/planController.js`
  - `server/src/controllers/subscriptionController.js`
  - `server/src/controllers/treeController.js`
  - `server/src/controllers/memberController.js`
  - `server/src/routes/adminRoutes.js`
  - `server/src/routes/subscriptionRoutes.js`
  - `server/src/app.js`
  - `server/src/validation/bodySchemas.js`
  - `client/lib/i18n/messages.ts`
  - `client/lib/i18n/provider.tsx`
  - `client/components/AppProviders.tsx`
  - `client/app/layout.tsx`
  - `client/components/TopNav.tsx`
  - `client/app/pricing/page.tsx`
  - `client/app/page.tsx`
  - `client/app/tree/[id]/page.tsx`
  - `client/lib/api.ts`
  - `client/types/index.ts`
  - `docs/API.md`
  - `ProjectTillNow.txt`
- Database schema state:
  - Added `Plan` collection/schema with pricing, limits, features, active state.
  - Added `Subscription` collection/schema linked to user + plan, with lifecycle status and billing window.
  - Existing `User`, `FamilyTree`, `Member` schemas remain intact.
  - Tree/member write flows now consult subscription state for enforced quotas.
- Pending SaaS upgrades:
  - Integrate real payment gateway/webhook flow (Stripe/Razorpay) and trusted payment reference verification.
  - Add invoice/history endpoints and subscription timeline UI.
  - Add trial plans/coupons/proration support.
  - Add scheduled background expiry sweep + usage analytics snapshots.
  - Add automated integration tests for subscription lifecycle and limit enforcement edge cases.
- Runtime status:
  - Client build: passed.
  - Server build/syntax: passed.
  - No duplicate business-rule branches introduced for plan checks (centralized in subscription service).

[2026-02-28 | Major Update 18 | PHASE: PAYMENT GATEWAY INTEGRATION + PUBLIC TREE SHARING]
- Continued from pending/new features only; completed modules were not regenerated.
- SaaS limits/subscription usage system preserved; tree engine rendering/navigation logic preserved.

Stripe payment integration (test mode)
1) SDK and environment
- Installed Stripe SDK in server (`stripe` package).
- Added environment variables:
  - `STRIPE_SECRET_KEY`
  - `STRIPE_WEBHOOK_SECRET`
  - `STRIPE_CURRENCY`
- Updated both root and server env templates accordingly.

2) Payment APIs
- Added `POST /api/payment/create-checkout-session`:
  - Auth protected.
  - Accepts `planId` (and optional `billingCycle`).
  - Validates plan exists and active.
  - Creates Stripe Checkout Session.
  - Returns `sessionId` + redirect `url`.
- Added `POST /api/payment/webhook`:
  - Mounted with raw-body parser for Stripe signature verification.
  - Verifies Stripe signature via `STRIPE_WEBHOOK_SECRET`.
  - Handles successful checkout events.
  - Activates subscription only after verified paid event.

3) Webhook activation logic
- On successful verified paid session:
  - validates user/plan metadata
  - validates paid amount matches selected plan/billing cycle
  - creates/updates subscription
  - sets `status=active`
  - sets `startDate` + computed `endDate`
  - stores `paymentReference`
- Subscription activation is webhook-only (manual subscribe endpoint now blocked).

4) Anti-spoofing + event logging
- Added `PaymentEvent` model for idempotent event logging:
  - unique `stripeEventId`
  - status lifecycle (`received`, `processed`, `ignored`, `failed`, `duplicate`)
- Prevented spoof/invalid activation with checks:
  - valid Stripe signature
  - valid metadata object ids
  - active plan existence
  - amount consistency verification
  - payment status = paid
- Added console payment event logs for processing observability.
- Added retry-safe behavior for previously failed webhook events.

5) Duplicate subscription protection
- `Subscription` model now enforces:
  - partial unique active subscription per user
  - sparse unique (`userId`, `paymentReference`) pair
- Combined with `PaymentEvent` idempotency, duplicate activation entries are prevented.

Public tree sharing + slug system
1) Slug system
- Added `slug` field to `FamilyTree` schema (unique, indexed, SEO-friendly format).
- Added slug utility for unique generation with collision handling.
- New trees now receive unique slug at creation.
- Existing trees get lazy backfill slug assignment when loaded/listed.

2) Public API by slug
- Added public read endpoint:
  - `GET /api/public/tree/:slug`
- Access rule:
  - allowed if tree is public
  - or valid tree password provided (`x-tree-password`)
- Reuses existing secure tree-access token/password middleware flow.

3) Frontend public route
- Added public tree page:
  - `/tree/public/[slug]`
- Supports:
  - unauthenticated viewing
  - password prompt for private trees
  - focus loading + directional tree exploration
- Keeps tree engine untouched by using existing focus/tree canvas behavior.

4) Share system in authenticated tree page
- Added Share controls on tree page:
  - copy public link
  - basic social share buttons (Twitter, Facebook, WhatsApp)
- Public link uses slug route format.

Frontend payment flow updates
- Pricing page subscribe now calls checkout API (`/api/payment/create-checkout-session`) and redirects user to Stripe checkout.
- Stripe success URL configured to `/dashboard?payment=success`.
- Dashboard now detects payment success query param and shows success notice.
- Added `/dashboard` route alias to keep success redirect stable.

Routing/middleware updates
- Added payment routes and public tree routes to app router.
- Mounted webhook as raw-body route before JSON/sanitization middleware to preserve Stripe signature verification correctness.

Documentation updates
- `docs/API.md` updated with:
  - payment checkout endpoint
  - webhook behavior + validation/idempotency notes
  - public slug endpoint and access rules
- `README.md` updated with Stripe env requirements and local webhook forwarding command.

Validation & runtime checks
- Server build/syntax passed: `cd server && npm run build`.
- Server app load passed: `node -e "require('./src/app')"`.
- Client build passed: `cd client && npm run build`.
- Webhook route sanity test passed (invalid signature path):
  - POST `/api/payment/webhook` returned `400` with `Invalid Stripe webhook signature.`

[2026-02-28 | Session Close Checklist]
- Completed modules:
  - Stripe checkout-session creation API.
  - Verified Stripe webhook with subscription activation flow.
  - Webhook-only activation enforcement (manual activation blocked).
  - Payment event logging + idempotency + anti-spoof validation.
  - Public slug-based tree sharing backend + frontend route.
  - Share actions (copy/social) and dashboard payment-success UX.
- File changes:
  - `server/package.json`
  - `server/package-lock.json`
  - `server/src/models/PaymentEvent.js`
  - `server/src/models/FamilyTree.js`
  - `server/src/models/Subscription.js`
  - `server/src/utils/treeSlug.js`
  - `server/src/controllers/paymentController.js`
  - `server/src/controllers/treeController.js`
  - `server/src/controllers/subscriptionController.js`
  - `server/src/middleware/treeAccessMiddleware.js`
  - `server/src/routes/paymentRoutes.js`
  - `server/src/routes/publicTreeRoutes.js`
  - `server/src/app.js`
  - `server/src/validation/bodySchemas.js`
  - `client/types/index.ts`
  - `client/lib/api.ts`
  - `client/app/pricing/page.tsx`
  - `client/app/page.tsx`
  - `client/app/dashboard/page.tsx`
  - `client/components/TopNav.tsx`
  - `client/app/login/page.tsx`
  - `client/app/register/page.tsx`
  - `client/app/tree/[id]/page.tsx`
  - `client/app/tree/public/[slug]/page.tsx`
  - `client/lib/i18n/messages.ts`
  - `.env.example`
  - `server/.env.example`
  - `docs/API.md`
  - `README.md`
  - `ProjectTillNow.txt`
- Database schema state:
  - `FamilyTree` now includes `slug` (unique/indexed) for public sharing URLs.
  - `Subscription` now includes additional uniqueness constraints to avoid duplicate activation records.
  - Added `PaymentEvent` collection for webhook idempotency and payment event audit logs.
  - Existing tree/member/subscription limit logic remains intact and unchanged in core behavior.
- Pending future upgrades:
  - Add Stripe customer portal + invoices endpoint and frontend billing-history view.
  - Add webhook event replay/admin monitoring dashboard for failed events.
  - Add automated integration tests for payment lifecycle + webhook retries + slug access flows.
  - Optional: introduce dedicated analytics for shared-link views and conversion funnels.
- Runtime status:
  - Server build: passed.
  - Client build: passed.
  - Webhook verification route: confirmed reachable and signature-validated error path works.
  - Tree engine remains unaffected.

[2026-02-28 | Major Update 18 - Addendum]
- Payment webhook error-response hardening:
  - `server/src/controllers/paymentController.js` now returns explicit `error.statusCode` values (e.g., 400/404) when available instead of falling through to generic 500.
- Revalidated backend syntax/build after hardening (`cd server && npm run build`): passed.

[2026-02-28 | Major Update 19 | PHASE: ENTERPRISE UPGRADE]
- Continued only from pending/new work; existing Stripe/SaaS/tree-engine modules were preserved.

Enterprise features completed
1) Email system (production-ready SMTP)
- Added Nodemailer SMTP service (`server/src/utils/emailService.js`) using env vars:
  - `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASS`, `EMAIL_FROM`
- Register now generates verification token + sends verification email.
- Login blocks unverified users.
- Added email verification APIs:
  - `GET /api/v1/auth/verify-email?token=...`
  - `POST /api/v1/auth/verify-email`
- Added password reset email/token APIs:
  - `POST /api/v1/auth/password-reset/request`
  - `POST /api/v1/auth/password-reset/confirm`
- Added user-facing auth recovery pages:
  - `/verify-email`
  - `/forgot-password`
  - `/reset-password`
- Stripe webhook success path now sends:
  - subscription confirmation email
  - payment success email

2) Audit log system
- Added `AuditLog` model with fields:
  - `userId`, `action`, `entityType`, `entityId`, `metadata`, `timestamp`
- Added audit logging utility (`createAuditLog`).
- Implemented logs for:
  - tree creation (`tree_create`)
  - member addition (`member_add`)
  - member deletion (`member_delete`)
  - plan purchase on successful Stripe webhook (`plan_purchase`)
  - subscription cancellation (`subscription_cancel`)
- Added admin paginated endpoint:
  - `GET /api/v1/admin/audit-logs`

3) Activity notifications system
- Added `Notification` model with fields:
  - `userId`, `message`, `isRead`, `createdAt` (+ metadata for context)
- Triggered notifications for:
  - subscription activated
  - plan expiring in 7 days
  - tree shared publicly
  - member added
- Added notification APIs:
  - `GET /api/v1/notifications`
  - `PUT /api/v1/notifications/read/:id`
- Added notification bell dropdown in navbar with unread count and mark-as-read behavior.

4) Backup system
- Added secure full export endpoint:
  - `GET /api/v1/tree/:id/export-full` (owner-protected)
- Added import endpoint:
  - `POST /api/v1/tree/import`
- Import validates payload structure/references and runs in MongoDB transaction.
- Import flow preserves relation mappings and enforces SaaS plan limits.

5) Performance monitoring
- Added request logging + timing middleware:
  - logs method, URL, status, duration, request id
  - warns for slow requests (`REQUEST_SLOW_THRESHOLD_MS`)
- Added centralized error logger utility and integrated it into error middleware.

6) API versioning + centralized router
- Added centralized API v1 router: `server/src/routes/apiV1/index.js`.
- Mounted all active routes under `/api/v1`.
- Kept `/api` as compatibility alias to prevent breaking existing clients.
- Stripe webhook remains mounted explicitly for `/api/v1/payment/webhook` (and compatibility path).

Frontend alignment updates
- API base default switched to `/api/v1`.
- Added verification/reset client API methods.
- Added login "Forgot password" entry.
- Added new auth recovery pages (verify/reset/forgot).
- Updated navbar notification icon rendering to SVG and preserved i18n/language switching.

Docs/config updates
- Updated env examples to include SMTP and performance env vars.
- Updated `README.md` and `docs/API.md` to reflect `/api/v1` and enterprise endpoint coverage.

Runtime validation
- Server build/syntax: passed (`cd server && npm run build`).
- Server app load: passed (`node -e "require('./src/app')"`).
- Client production build: passed (`cd client && npm run build`).
- Stripe webhook route sanity: reachable at `/api/v1/payment/webhook` (returns expected config error when Stripe secrets are absent).
- Tree engine flows preserved (focus/relations/graph/public-share behavior unchanged).

[2026-02-28 | Session Close Checklist]
- Completed modules:
  - Production SMTP email verification + password reset + payment/subscription mailers.
  - Enterprise audit logging with admin pagination API.
  - User activity notifications system with navbar bell + read tracking.
  - Full tree backup export/import (validated, transaction-safe import).
  - Request/performance/error monitoring middleware/utilities.
  - API versioning with centralized `/api/v1` routing and compatibility alias.
  - User-facing email verification and password reset frontend routes.
- File changes:
  - `server/src/app.js`
  - `server/src/routes/apiV1/index.js`
  - `server/src/routes/notificationRoutes.js`
  - `server/src/controllers/notificationController.js`
  - `server/src/models/Notification.js`
  - `server/src/models/AuditLog.js`
  - `server/src/utils/auditLogger.js`
  - `server/src/utils/notificationService.js`
  - `server/src/utils/errorLogger.js`
  - `server/src/middleware/performanceLoggingMiddleware.js`
  - `server/src/middleware/errorMiddleware.js`
  - `server/src/utils/emailService.js`
  - `server/src/controllers/authController.js`
  - `server/src/models/User.js`
  - `server/src/routes/authRoutes.js`
  - `server/src/validation/bodySchemas.js`
  - `server/src/controllers/paymentController.js`
  - `server/src/controllers/subscriptionController.js`
  - `server/src/controllers/treeController.js`
  - `server/src/controllers/memberController.js`
  - `server/src/controllers/adminController.js`
  - `server/src/routes/adminRoutes.js`
  - `server/src/routes/coreTreeRoutes.js`
  - `server/src/middleware/requestSecurityMiddleware.js`
  - `server/.env.example`
  - `.env.example`
  - `client/lib/api.ts`
  - `client/types/index.ts`
  - `client/components/TopNav.tsx`
  - `client/app/login/page.tsx`
  - `client/app/register/page.tsx`
  - `client/app/forgot-password/page.tsx`
  - `client/app/verify-email/page.tsx`
  - `client/app/reset-password/page.tsx`
  - `README.md`
  - `docs/API.md`
  - `ProjectTillNow.txt`
- Database schema state:
  - Added `AuditLog` collection/schema.
  - Added `Notification` collection/schema.
  - `User` schema extended with email verification + password reset token/hash expiry fields.
  - Existing `Plan`, `Subscription`, `PaymentEvent`, `FamilyTree`, `Member` schemas remain intact and compatible.
  - Subscription activation remains webhook-only and idempotent.
- Pending future enterprise tasks:
  - Add scheduled background jobs/queue for proactive plan-expiry notifications and email retries.
  - Add delivery-state tracking for outgoing emails (sent/failed/retry metadata).
  - Add audit-log retention policies and export tooling for compliance workflows.
  - Add automated integration tests for webhook-to-subscription-to-notification/email chain.
  - Add admin UI surfaces for audit logs and notification history.
  - Add backup import dry-run mode with diff preview.

[2026-03-01 | Minor Update | Dev-Only Email Verification Bypass]
- Completed modules:
  - Added development-only bypass for mandatory email verification during login.
  - Users with `isEmailVerified=false` can now log in when `NODE_ENV=development`.
  - Verification token generation, verification APIs, and production verification enforcement remain unchanged.
- File changes:
  - `server/src/controllers/authController.js`
- Database schema state:
  - No schema changes.
- Pending tasks:
  - Optional: add automated tests covering login behavior differences between `development` and `production` modes.
- Runtime status:
  - Server syntax/build check passed (`cd server && npm run build`).

[2026-03-01 | Minor Update | Login Flow Token Handling Fix]
- Completed modules:
  - Updated frontend login flow to store JWT token exactly with:
    - `localStorage.setItem("token", response.token);`
  - Updated API token handling to send auth header using localStorage token key:
    - `Authorization: Bearer ${localStorage.getItem("token")}`
  - Kept backend auth logic unchanged.
  - Confirmed backend login success response already includes `token` field.
- File changes:
  - `client/app/login/page.tsx`
  - `client/lib/api.ts`
- Database schema state:
  - No schema changes.
- Pending tasks:
  - Optional future cleanup: migrate remaining auth gate checks to consistently prefer localStorage `token` key across all UI guards.
- Runtime status:
  - Client production build passed (`cd client && npm run build`).

[2026-03-01 | Minor Update | Explicit Login Fetch Flow]
- Completed modules:
  - Login form now sends explicit request to:
    - `POST http://localhost:5000/api/v1/auth/login`
  - Login submit uses direct `fetch` in client component handleSubmit.
  - Added explicit `event.preventDefault()` in submit flow (retained).
  - On successful login:
    - parses JSON response
    - stores token with `localStorage.setItem("token", data.token)`
- File changes:
  - `client/app/login/page.tsx`
- Database schema state:
  - No schema changes.
- Pending tasks:
  - None for this scoped login-page change.
- Runtime status:
  - Client production build passed (`cd client && npm run build`).

=== MongoDB Reinstallation Recovery ===
Date: 2026-03-01

Connection status
- `server/.env` verified with:
  - `MONGO_URI=mongodb://127.0.0.1:27017/memoryvault`
- Added temporary startup DB log in `server/src/server.js`:
  - `console.log("Connected DB Name:", mongoose.connection.name);`
- Mongo connectivity confirmed.
- Confirmed connected database name: `memoryvault`.
- Note: local port `5000` was already in use by an existing process during direct restart check, but DB connection and DB name logging were verified successfully.

Collections state
- Verified required collections in `memoryvault`:
  - `users` (present)
  - `familytrees` (present)
  - `members` (present)
  - `subscriptions` (present)
  - `paymentevents` (present)
  - `notifications` (present)
  - `auditlogs` (present)
- No seeding performed.
- Empty-state handling remains acceptable if collections are newly created with no records.

Index verification
- User:
  - `email` unique index: present.
- Subscription:
  - unique active-subscription constraint (`{ userId: 1, status: 1 }` with partial filter `status: "active"`): present.
- Member:
  - `treeId` index: present.
- FamilyTree:
  - `owner` index: present.
  - `slug` unique index: present.
- Since required indexes were already present, no index recreation was required.

Auth flow status
- Register test user: success (`201`).
- Login test user: success (`200`), JWT token issued.
- `tokenVersion` check:
  - before login: `0`
  - after login: `1`
  - increment confirmed.
- Protected route check:
  - `GET /api/v1/trees` with bearer token: `200` (returned array, empty is valid).

Fixes applied during recovery
- `server/src/server.js`:
  - Added temporary DB-name startup log after DB connection.
- `server/src/middleware/authMiddleware.js`:
  - Fixed Mongo projection in token user lookup (`select(...)`) by removing mixed include/exclude projection that caused auth resolution failure on protected routes.
  - This was a minimal connectivity/recovery-safe fix; no business-rule/auth-policy behavior was changed.

Final validation
- `cd server && npm run build` -> passed.
- `cd client && npm run build` -> passed.

Recovery outcome
- Development environment is stable.
- MongoDB connectivity is healthy.
- Required collections and critical indexes are in place.
- Auth and protected-route access flow is operational.

[2026-03-01 | Major Update 20 | PHASE: ENTERPRISE UPGRADE - Verification & Stability Pass]
- Continued from existing completed modules only; no regeneration or rewrite of Stripe/SaaS/tree engine/business logic.

Enterprise module status (confirmed)
1) Email system (production-ready SMTP)
- Nodemailer SMTP service is active via:
  - `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASS`, `EMAIL_FROM`
- Verified implemented flows:
  - email verification on register
  - password reset email with token
  - subscription confirmation email
  - payment success email

2) Audit log system
- `AuditLog` model present with required fields:
  - `userId`, `action`, `entityType`, `entityId`, `metadata`, `timestamp`
- Logging hooks confirmed for:
  - tree creation
  - member addition
  - member deletion
  - plan purchase
  - subscription cancellation
- Admin paginated endpoint confirmed:
  - `GET /api/v1/admin/audit-logs`
  - Legacy compatibility path also reachable: `GET /api/admin/audit-logs`

3) Activity notifications system
- `Notification` model present with required fields (`userId`, `message`, `isRead`, `createdAt`).
- Trigger flows confirmed for:
  - subscription activated
  - plan expiring in 7 days
  - tree shared publicly
  - member added
- Endpoints confirmed:
  - `GET /api/v1/notifications`
  - `PUT /api/v1/notifications/read/:id`
  - Legacy compatibility path also reachable under `/api/notifications`.
- Frontend notification bell confirmed in navbar.

4) Backup system
- Endpoints confirmed:
  - `GET /api/v1/tree/:id/export-full` (secured)
  - `POST /api/v1/tree/import` (validated + transaction-safe)
  - Legacy compatibility paths also reachable under `/api/tree/*`.

5) Performance monitoring
- Request performance logging middleware active.
- Basic timing/slow-request logging active.
- Centralized error logging utility active in error middleware.

6) API versioning + centralized router
- Centralized `/api/v1` router is active.
- Compatibility alias `/api` is preserved and functional.

Compatibility/runtime verification performed
- Endpoint reachability checks (legacy + v1) confirmed for:
  - audit logs
  - notifications
  - tree export/import
- Stripe webhook unaffected:
  - `/api/payment/webhook` -> reachable, signature path returns expected `400`
  - `/api/v1/payment/webhook` -> reachable, signature path returns expected `400`
- No breaking changes introduced in this pass.

Build validation
- Server build/syntax: passed (`cd server && npm run build`).
- Client production build: passed (`cd client && npm run build`).

File changes in this update
- `ProjectTillNow.txt` (append-only verification/update block).

Database schema state
- No schema changes in this pass.
- Existing enterprise and SaaS/Stripe schema state remains intact.

Pending future enterprise tasks
- Add scheduled worker for proactive plan-expiry notifications and email retries.
- Add durable email delivery status tracking and retry policy visibility.
- Add automated integration test suite for email + audit + notification + webhook chains.
- Add admin UI for audit-log filtering/export and notification observability.
- Add backup import dry-run + schema-diff preview before commit.

[2026-03-01 | Major Update 21 | PHASE: PRODUCTION HARDENING]
- Continued with hardening-only scope. No feature regeneration. No Stripe/SaaS/tree engine behavior changes.

Production hardening summary
1) Security finalization
- Added strict boot-time environment validation via `server/src/config/validateEnv.js`.
- Server now fails startup if missing:
  - `JWT_SECRET`
  - `MONGO_URI`
  - `STRIPE_SECRET_KEY`
  - SMTP vars (`SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASS`, `EMAIL_FROM`) when `EMAIL_ENABLED=true`.
- Added production CORS hardening:
  - App now resolves allowed origin from `CLIENT_URL` (with legacy fallback to `FRONTEND_URL` for compatibility).
  - Origin guard remains enforced for unsafe methods.
- Removed direct `console.log/warn/error` usage from runtime `server/src/**` and routed logs through centralized logger.
- Stack traces remain hidden in production responses (error middleware unchanged behavior).

2) Database safety confirmation
- Verified critical schema constraints/indexes remain enforced:
  - FamilyTree `slug` unique constraint/index present.
  - Subscription unique active-per-user partial index present.
  - User `email` unique index present.
  - Member `treeId` index present.
- No destructive collection/index operations were performed.

3) Rate limiting strategy
- Implemented tiered limit strategy in `server/src/app.js`:
  - Auth routes: strict (`AUTH_RATE_LIMIT_MAX`).
  - Payment routes: medium (`PAYMENT_RATE_LIMIT_MAX`, `PAYMENT_RATE_LIMIT_WINDOW_MS`).
  - General API routes: normal (`RATE_LIMIT_MAX`, `RATE_LIMIT_WINDOW_MS`).
- General limiter now skips auth/payment paths so each route group uses intended tier only.

4) Production logger
- Added centralized logger utility: `server/src/utils/logger.js` with levels:
  - `info`, `warn`, `error`.
- Integrated logger into runtime paths:
  - failed login attempts (auth controller)
  - payment failures / webhook event lifecycle (payment controller)
  - tree deletion events (tree controller)
  - admin actions (admin controller)
  - request timing/slow logs (performance middleware)
  - startup/db logs and error logger utility

5) Health check endpoint hardening
- Updated `GET /api/v1/health` to return:
  - `status`
  - `uptime`
  - `timestamp`
  - `dbConnected` (derived from mongoose readyState)

6) Final optimization pass
- Removed runtime direct console statements from `server/src`.
- Kept middleware stack minimal and avoided duplicate effective rate-limit application.
- No deprecated Stripe API calls introduced; existing Stripe flow preserved.
- No circular dependency changes introduced in this pass.

Security verification
- Env validator behavior tested:
  - Missing `JWT_SECRET` -> startup validation failure.
  - Missing `MONGO_URI` -> startup validation failure.
  - Missing `STRIPE_SECRET_KEY` -> startup validation failure.
  - `EMAIL_ENABLED=true` + missing SMTP var -> startup validation failure.
  - `EMAIL_ENABLED=false` allows SMTP vars to be absent.
- CORS policy now resolves from `CLIENT_URL` (legacy-compatible fallback retained).
- Runtime error responses keep stack hidden in production mode.
- Stripe webhook path unaffected (invalid signature path still returns `400` with configured env).

Final readiness checklist
- Server build/syntax: passed (`cd server && npm run build`).
- Client production build: passed (`cd client && npm run build`).
- Client production build warnings: none observed in build output.
- Health endpoint verified with DB connected state and uptime payload.
- Critical indexes/constraints verified present.

Files changed in this hardening update
- `server/src/config/validateEnv.js` (new)
- `server/src/utils/logger.js` (new)
- `server/src/server.js`
- `server/src/config/db.js`
- `server/src/app.js`
- `server/src/routes/apiV1/index.js`
- `server/src/utils/errorLogger.js`
- `server/src/middleware/performanceLoggingMiddleware.js`
- `server/src/utils/emailService.js`
- `server/src/controllers/authController.js`
- `server/src/controllers/paymentController.js`
- `server/src/controllers/treeController.js`
- `server/src/controllers/adminController.js`
- `.env.example`
- `server/.env.example` (restored)
- `ProjectTillNow.txt`

Pending hardening follow-ups
- Optional: fail startup in production if `CLIENT_URL` is absent (currently legacy fallback to `FRONTEND_URL` retained for compatibility).
- Optional: add structured log transport/rotation (file sink or external collector) for long-term production retention.
- Optional: add CI check to enforce zero `console.*` usage in runtime server source.

[2026-03-01 | Env Setup Update | .env.example -> .env]
- Scope: environment setup only (no module regeneration, no business logic changes).
- Root `.env`:
  - Created from `.env.example` because it was missing.
  - Applied safe local dev defaults/placeholders:
    - `NODE_ENV=development`
    - `CLIENT_URL=http://localhost:3000`
    - `SERVER_URL=http://localhost:5000`
    - placeholder secrets for JWT/Stripe/SMTP.
- `server/.env`:
  - Already existed; not overwritten.
  - Filled only missing keys:
    - `CLIENT_URL`
    - `EMAIL_ENABLED`
    - `PAYMENT_RATE_LIMIT_WINDOW_MS`
    - `PAYMENT_RATE_LIMIT_MAX`
- Validation:
  - No duplicate keys.
  - No env syntax errors.
  - Required keys aligned with `server/src/config/validateEnv.js`.
  - `.env` paths confirmed in `.gitignore` (root + server).
  - Startup sanity check passed (`/api/v1/health` returned 200, `dbConnected=true`).

[2026-03-01 | Phase Update | FREE PLAN + RAZORPAY INTEGRATION]
- Scope followed: continued existing modules only, no regeneration.
- Payment/subscription stack migrated from Stripe to Razorpay.

Completed modules
1) Free plan default assignment
- Added enforced default Free plan provisioning (created/normalized if missing):
  - name: Free
  - priceMonthly: 0
  - priceYearly: 0
  - maxMembers: 10
  - maxTrees: 1
  - features: ["Basic Tree"]
  - isActive: true
  - isDefault: true
- On user registration, default subscription is created automatically.
- Existing users without active subscription now auto-fall back to Free when subscription state is resolved.

2) Plan limit logic fixes
- Tree creation guard updated:
  - first tree is allowed when `treesUsed = 0` and `maxTrees >= 1`.
- Member creation guard remains plan-driven by `maxMembers` and now returns exact Free-plan message:
  - "Free plan allows only 10 members. Upgrade to continue."
- Frontend tree UI now shows this message for Free-plan member-limit state.

3) Stripe removal
- Removed Stripe SDK usage and webhook processing.
- Removed webhook route mount and Stripe env validation.
- Removed Stripe checkout route flow.
- Removed Stripe-specific `PaymentEvent` model.

4) Razorpay integration
- Backend endpoints added:
  - `POST /api/v1/payment/create-order`
  - `POST /api/v1/payment/verify`
- `create-order`:
  - validates plan + cycle
  - creates Razorpay order
  - returns `orderId`, amount, currency, key id, plan meta.
- `verify`:
  - verifies HMAC signature using `RAZORPAY_KEY_SECRET`
  - validates order amount + order metadata consistency
  - activates subscription only after successful verification
  - stores `razorpay_payment_id` in subscription `paymentReference`
  - prevents duplicate activation via payment-reference checks + transaction guard.
- Frontend pricing flow:
  - subscribe click -> create order
  - opens Razorpay checkout script
  - success handler -> verify endpoint
  - on verified success -> redirects to `/dashboard?payment=success`.

File changes
- Server
  - `server/src/models/Plan.js`
  - `server/src/utils/subscriptionService.js`
  - `server/src/controllers/authController.js`
  - `server/src/controllers/paymentController.js`
  - `server/src/routes/paymentRoutes.js`
  - `server/src/validation/bodySchemas.js`
  - `server/src/app.js`
  - `server/src/config/validateEnv.js`
  - `server/src/controllers/subscriptionController.js`
  - `server/src/controllers/planController.js`
  - `server/src/models/PaymentEvent.js` (deleted)
  - `server/package.json`
  - `server/package-lock.json`
  - `server/.env.example`
  - `server/.env`
- Client
  - `client/lib/api.ts`
  - `client/types/index.ts`
  - `client/app/pricing/page.tsx`
  - `client/app/tree/[id]/page.tsx`
- Docs/Config
  - `docs/API.md`
  - `README.md`
  - `.env.example`
  - `.env`
  - `ProjectTillNow.txt`

Database schema state
- `Plan` schema:
  - added `isDefault: Boolean`.
  - unique partial index on `{ isDefault: true }` (single default plan guarantee).
- `Subscription` schema:
  - unchanged structure.
  - `paymentReference` now stores Razorpay payment id after verified payment.
- `PaymentEvent` schema/model:
  - removed (Stripe-specific; no longer used).

Validation/build/runtime checks
- Server build/syntax: passed (`cd server && npm run build`).
- Client production build: passed (`cd client && npm run build`).
- Stripe references removed from runtime source and env templates.
- Razorpay dependency installed and lockfile updated.

Pending tasks
- Add automated integration tests for:
  - registration -> free subscription auto-assignment,
  - tree/member limits (1 tree, 10 members on Free),
  - Razorpay verify idempotency/duplicate protection.
- Optional: add server-side payment reconciliation job for disputed/failed Razorpay payments.
- Optional: add admin-visible payment audit view for verified Razorpay transactions.

[2026-03-01 | Validation Addendum | Free + Razorpay Phase]
- Removed duplicate inline `isDefault` index declaration in `Plan` schema to avoid index duplication warning.
- Health smoke check successful: `GET /api/v1/health` returned `200` with `dbConnected=true`.

[2026-03-01 | Runtime Validation Addendum | Free Plan Limits]
- Added standalone-Mongo safety fallback in `server/src/utils/withMongoTransaction.js`:
  - if transactions are unsupported (non-replica-set MongoDB), handlers now run without transaction session.

Runtime smoke checks executed on updated server (`PORT=5001`)
1) Free subscription default
- Registered a fresh user.
- `GET /api/v1/subscription/my` returned active `Free` plan with:
  - `maxTrees = 1`
  - `maxMembers = 10`

2) Tree limit behavior
- First tree creation succeeded.
- Second tree creation returned `403` with:
  - `{"message":"Tree limit reached for plan Free. Upgrade your plan to create more trees."}`

3) Member limit behavior
- First 10 member creations succeeded.
- 11th member creation returned `403` with exact required message:
  - `{"message":"Free plan allows only 10 members. Upgrade to continue."}`

4) Free plan payment behavior
- `POST /api/v1/payment/create-order` with Free plan returned:
  - `{"message":"Selected plan does not require payment."}`
- Confirms Free plan does not trigger payment flow.

[2026-03-01 | Phase Update | UX REFINEMENT + TREE BEHAVIOR FIX]
- Scope followed: continued existing modules only, no regeneration.
- Free plan/subscription logic kept intact.

Completed modules
1) Delete member UX refinement
- Member Details modal deletion flow updated:
  - confirmation shown: "Are you sure you want to delete this member?"
  - if member has children, explicit choice prompt added:
    - delete only this member
    - delete entire subtree
- Existing delete API is used with `subtree=true|false` based on selected choice.
- Post-delete focus behavior updated:
  - if deleted member was focused, focus now shifts to parent (father/mother if available),
  - otherwise falls back to tree root member.

2) Tree node click behavior fix
- Node interaction updated in canvas:
  - clicking anywhere on a node now changes focus.
  - added small info icon `?` on each node.
  - clicking `?` opens Member Details modal.
- Removed old behavior where main node click opened details.

3) "My Trees" navigation fix
- Top navigation `My Trees` action updated:
  - if user has exactly 1 tree, it opens that tree directly.
  - if multiple trees exist, it routes to dashboard tree list (`/dashboard#my-trees`).

4) Root focus behavior fix
- Tree load behavior changed to always initialize focus from tree root context (`rootMember` / initial root fallback).
- Removed stale focus carry-over on reload that could keep previous member focus.
- Add Member modal remains tied to current focused node (`relatedMemberId = focusId`).

5) Dashboard plan display simplification
- Dashboard plan/usage section simplified to show only member usage:
  - `Members: X / maxMembers`
- Removed tree usage ratio display from dashboard plan cards.

6) Dashboard create/open UX changes
- Text updated:
  - `Create New Tree` -> `Create a Tree`.
- If user already has a tree:
  - create form is hidden,
  - single primary action shown: `My Tree` (opens tree directly).

7) Member detail route validation fix (400 -> 404)
- Backend member detail GET validation adjusted for relation endpoint behavior:
  - removed strict `memberId` express-validator check from GET relation/read routes,
  - added controller-level ObjectId guard returning `404 Member not found` for invalid member IDs.
- Result: invalid member detail path no longer returns 400; returns 404 as required.

File changes
- Client
  - `client/components/FocusTreeCanvas.tsx`
  - `client/app/tree/[id]/page.tsx`
  - `client/app/tree/public/[slug]/page.tsx`
  - `client/components/TopNav.tsx`
  - `client/app/page.tsx`
  - `client/lib/i18n/messages.ts`
- Server
  - `server/src/routes/memberRoutes.js`
  - `server/src/controllers/memberController.js`
- Docs/Progress
  - `ProjectTillNow.txt`

Database schema state
- No schema changes in this UX refinement phase.
- Existing Free-plan + Razorpay schema state remains intact.

Validation/build/runtime checks
- Server build/syntax: passed (`cd server && npm run build`).
- Client production build: passed (`cd client && npm run build`).
- Runtime smoke checks on updated server (`PORT=5001`) passed:
  - Tree creation works.
  - Free plan limits still enforced:
    - 2nd tree -> `403`.
    - 11th member -> `403` with required Free-plan message.
  - Invalid member details endpoint:
    - `GET /api/v1/trees/:id/members/not-a-valid-id/relations` -> `404` (`Member not found`).

Pending tasks
- Optional: replace prompt-based delete choice with a dedicated modal containing two explicit buttons for clearer UX.
- Optional: add end-to-end UI tests for node info icon behavior and post-delete focus fallback.
- Optional: add a small loading state indicator on `My Trees` nav action during direct tree resolution.

[2026-03-01 | Phase Update | AUTO ROOT MEMBER INITIALIZATION]
- Scope followed: continued existing modules only, no regeneration.
- Free-plan/subscription logic kept intact.

Completed modules
1) Root member auto-creation on tree creation
- Updated tree creation flow to auto-create a root member immediately after `FamilyTree` creation.
- Root member payload now uses logged-in user name:
  - `name = req.user.name`
  - `treeId = createdTree._id`
  - `createdBy = req.user._id`
  - `isRoot = true`
- New tree response now returns initialized root context:
  - `memberCount = 1`
  - `rootMember` set
  - `rootMemberId` set
  - `initialFocusMember` set to root.

2) `rootMemberId` compatibility field in `FamilyTree`
- Added `rootMemberId` field to `FamilyTree` schema while keeping existing `rootMember` for backward compatibility.
- Added schema pre-validate sync so both fields always mirror the same root value.
- Safe tree serializer now always emits both `rootMember` and `rootMemberId` in sync.

3) Single-root safety enforcement
- Added `isRoot` boolean to `Member` schema.
- Added unique partial index on `{ treeId, isRoot }` where `isRoot: true` to guarantee one root per tree.
- Create/import/delete/fallback flows now maintain `isRoot` consistency.

4) Old-tree fallback safety (no breakage)
- Added fallback root resolution for trees missing root references:
  - on tree payload build, if no root is set and members exist,
  - first created member is assigned as root automatically,
  - tree root fields are persisted,
  - member `isRoot` flags are corrected.
- Ensures legacy trees without `rootMemberId` still load with deterministic root focus.

5) Frontend root focus initialization update
- Tree detail page now prefers `rootMemberId` (then `rootMember`, then `initialFocusMember`) when initializing focus.
- Public tree page updated with same root-priority focus logic.
- Removed old first-member empty-state copy tied to manual first-person creation.

File changes
- Server
  - `server/src/models/FamilyTree.js`
  - `server/src/models/Member.js`
  - `server/src/controllers/treeController.js`
  - `server/src/controllers/memberController.js`
  - `server/src/utils/subscriptionService.js`
- Client
  - `client/types/index.ts`
  - `client/app/tree/[id]/page.tsx`
  - `client/app/tree/public/[slug]/page.tsx`
- Docs/Progress
  - `ProjectTillNow.txt`

Database schema state
- `FamilyTree`:
  - `rootMember` (existing)
  - `rootMemberId` (new)
  - pre-validate compatibility sync keeps both aligned.
- `Member`:
  - `isRoot: Boolean` (new, default `false`).
  - unique partial index added:
    - `{ treeId: 1, isRoot: 1 }` with partial filter `{ isRoot: true }`.
- Existing Free plan + Razorpay schema remains unchanged.

Validation/build/runtime checks
- Server build/syntax: passed (`cd server && npm run build`).
- Client production build: passed (`cd client && npm run build`).
- Runtime smoke checks (updated code path via isolated app instance) passed:
  - Tree creation auto-creates root member.
  - Created tree response includes non-null `rootMember`, `rootMemberId`, and `initialFocusMember`.
  - Member list shows 1 member (owner name) with `isRoot: true`.
  - Legacy fallback verified by clearing tree root fields manually:
    - `GET /api/v1/trees/:id` auto-selected first created member as root,
    - persisted root fields,
    - set `isRoot: true` on fallback root.

Pending tasks
- Optional: add migration script to backfill `rootMemberId` and `isRoot` for all existing trees in bulk (current runtime fallback already self-heals on access).
- Optional: add integration tests for:
  - tree create -> root auto-init,
  - legacy root fallback,
  - delete-root -> fallback-root reassignment.

[2026-03-01 | Phase Update | ACCOUNT PROFILE + ROOT MEMBER LINK]
- Scope followed: continued existing modules only, no regeneration.
- Free plan, Razorpay, and existing tree engine flows were preserved.

Completed modules
1) User model/account profile support
- Added `profileImage` field to `User` schema.
- `updatedAt` remains available via existing Mongoose `timestamps: true` (already active).
- Auth/user payload sanitization now includes `profileImage` so frontend account state stays in sync.

2) Root member user linking
- Added `linkedUserId` (`ObjectId`, ref `User`) to `Member` schema.
- Root creation now links root member to the logged-in user:
  - tree create root member: `isRoot=true`, `linkedUserId=user._id`
  - import root assignment: root member set `isRoot=true`, `linkedUserId=user._id`
  - legacy root fallback/self-heal paths now set root `linkedUserId` to tree owner.
- Single-root safety is preserved by existing unique partial index on `{ treeId, isRoot: true }`.

3) Account API system (`/api/v1/account`)
- Added new secured endpoints:
  - `GET /api/v1/account`
  - `PUT /api/v1/account/update`
  - `PUT /api/v1/account/password`
  - `DELETE /api/v1/account`
- Account update supports:
  - name update
  - profile image upload
- Password update behavior:
  - validates current password
  - rotates token/session state (`tokenVersion`, `activeTokenId`, `csrfTokenHash`) so re-login is required.
- Account delete behavior:
  - validates current password
  - deletes owned trees + members
  - deletes subscription + notifications
  - nulls historical audit-log user references
  - removes user record
  - unlinks any residual `Member.linkedUserId` references.

4) Tree page enhancement
- Added conditional `Edit My Profile` button on tree page when focused node satisfies:
  - `isRoot === true`
  - `linkedUserId === loggedInUserId`
- Button routes to `/account`.

5) Security hardening for root ownership
- Added root-member edit guard in member mutation controller paths:
  - update member
  - delete member
  - relation mutation
  - relation-attached create operations affecting root member
- Enforcement rule:
  - if target member is root and has `linkedUserId`, only that linked user can edit.
- Result: admin/other users cannot modify another user?s linked root member.

6) Frontend account page (`/account`)
- Added account management route with features:
  - view profile data
  - edit name
  - upload profile image
  - change password
  - delete account (confirmation + password)
- Added navbar link to `Account`.
- Added client API wrappers and type contracts for new account endpoints.

File changes
- Server
  - `server/src/models/User.js`
  - `server/src/models/Member.js`
  - `server/src/controllers/authController.js`
  - `server/src/controllers/treeController.js`
  - `server/src/controllers/memberController.js`
  - `server/src/controllers/accountController.js` (new)
  - `server/src/routes/accountRoutes.js` (new)
  - `server/src/routes/apiV1/index.js`
  - `server/src/middleware/authMiddleware.js`
  - `server/src/validation/bodySchemas.js`
- Client
  - `client/types/index.ts`
  - `client/lib/api.ts`
  - `client/components/TopNav.tsx`
  - `client/app/tree/[id]/page.tsx`
  - `client/app/account/page.tsx` (new)
- Docs/Progress
  - `ProjectTillNow.txt`

Database schema state
- `User`
  - added: `profileImage: String | null`
  - `updatedAt`/`createdAt` still managed by timestamps.
- `Member`
  - added: `linkedUserId: ObjectId | null` (ref `User`)
  - existing `isRoot` retained.
  - index added: `{ linkedUserId: 1 }`
  - existing unique partial root index retained: `{ treeId: 1, isRoot: 1 }` with `{ isRoot: true }`.
- `FamilyTree`, `Plan`, `Subscription`, Razorpay/payment schema state unchanged in this phase.

Validation/build/runtime checks
- Build checks:
  - `cd server && npm run build` -> passed.
  - `cd client && npm run build` -> passed.
- Runtime smoke checks (isolated in-process server instance with current code) passed:
  - Tree root created with `isRoot=true` and `linkedUserId=user._id`.
  - `GET/PUT /api/v1/account` works.
  - account name update syncs linked root member name.
  - non-owner admin edit attempt on linked root member returns `403` with expected message.
  - root owner edit still succeeds.
  - password update invalidates old token (old token returns `401`).
  - account deletion succeeds and token becomes invalid.

Pending tasks
- Optional: add account profile image removal endpoint/action (set to null) for explicit photo reset UX.
- Optional: add integration tests for account lifecycle (update/password/delete) and root-owner guard cases.
- Optional: add background cleanup for orphan upload files on profile image replacement/deletion.

[2026-03-01 | Bug Fix Update | Logout -> Login Button Flow]
- Scope followed: no module regeneration, only focused auth UX/logic fix.

Completed modules
1) Post-logout login flow stabilization
- Standardized login page auth flow to use shared API/auth utilities only.
- Removed legacy direct token handling from login page submit/redirect behavior.

2) Legacy token cleanup on logout
- Updated auth cleanup to remove both keys:
  - `memoryvault_token` (current)
  - `token` (legacy)
- Prevents stale legacy session values from interfering after logout.

3) Request auth-header consistency hardening
- Updated client API request auth selection to prefer canonical token and only fallback to legacy token.
- Applied same logic to public-tree fetch auth header path.

File changes
- `client/lib/auth.ts`
- `client/lib/api.ts`
- `ProjectTillNow.txt`

Database schema state
- No database schema changes in this bug-fix update.
- Existing Free plan, Razorpay, root-member linking, and account schema state unchanged.

Validation/build/runtime checks
- `cd client && npm run build` -> passed.
- `cd server && npm run build` -> passed.

Pending tasks
- Optional: remove legacy `token` fallback in client API entirely after one release cycle if backward compatibility is no longer needed.
- Optional: add frontend regression test covering login -> logout -> login navigation flow.

[2026-03-01 | Runtime Fix | Next.js Missing Chunk (./24.js)]
- Scope followed: no module regeneration and no feature changes; runtime/dev-environment fix only.

Completed modules
1) Next dev runtime stabilization
- Investigated `Cannot find module './24.js'` server error from Next dev runtime.
- Identified root cause: multiple concurrent `next dev` instances were writing to the same `client/.next` directory (`3000`, `3001`, `3005`), producing inconsistent chunk references.
- Stopped duplicate Next dev processes.
- Cleared stale build artifacts (`client/.next`) and restarted a single dev instance.
- Verified `/dashboard` and not-found route no longer show chunk-module error.

File changes
- `ProjectTillNow.txt`

Database schema state
- No database/schema changes.

Validation/build/runtime checks
- Runtime validation passed:
  - Single Next dev instance serves `/dashboard` without `Cannot find module './24.js'`.
  - Not-found route returns normal `404` without chunk-load server error.

Pending tasks
- Keep only one `next dev` process running per workspace path.
- Optional hardening: introduce a `dev:clean` script (`rimraf .next && next dev`) if this recurs frequently.

[2026-03-01 | Phase Update | MEMBER DELETE + RELATIONSHIP MANAGEMENT]
- Scope followed: continued existing modules only, no regeneration.
- Free plan logic, Razorpay integration, and root-owner protection flows were preserved.

Completed modules
1) Member Details modal action split
- Added explicit action buttons in member modal:
  - `Edit Member`
  - `Remove Relationship`
  - `Delete Member`
- Modal now renders action-specific sections for better UX clarity.

2) Delete member flow enhancement
- Root delete protection enforced in UI and backend:
  - delete action hidden for `isRoot` members.
  - backend now blocks root deletion with `403` (`Root member cannot be deleted.`).
- Delete section behavior:
  - no-children -> direct delete action.
  - has-children -> two explicit options:
    - `Delete only this member`
    - `Delete entire subtree`
- Backend non-subtree delete now relinks children to available parent (`fatherId`/`motherId` of deleted member) where possible before deletion.

3) Remove relationship API added
- New endpoint implemented:
  - `PUT /api/v1/member/remove-relation`
- Payload:
  - `memberId`
  - `relationType` (`spouse` | `sibling` | `parent` | `child`)
  - `relatedMemberId`
- Behavior:
  - resolves tree context from `memberId`
  - enforces owner/admin tree write access
  - removes relation bidirectionally/integrity-safe via existing relation mutation engine + rebuild
  - validates parent-removal consistency (`parent` must be actual father/mother relation)

4) Relationship removal UI
- In member modal `Remove Relationship` section:
  - shows grouped relationship lists (parents, spouses, siblings, children)
  - each relation row has remove action with confirmation
  - removal calls new API and refreshes detail + focused tree context
- Existing relation engine form retained for advanced connect/disconnect operations.

5) Focus safety after mutations
- After relationship removal and member deletion:
  - detail state and focus bundle are refreshed
  - focus fallback remains safe (parent -> root -> null)
  - avoids blank-canvas/crash regressions.

File changes
- Server
  - `server/src/controllers/memberController.js`
  - `server/src/routes/coreMemberRoutes.js`
  - `server/src/middleware/memberContextMiddleware.js`
  - `server/src/validation/bodySchemas.js`
- Client
  - `client/app/tree/[id]/page.tsx`
  - `client/lib/api.ts`
  - `client/types/index.ts`
- Docs/Progress
  - `ProjectTillNow.txt`

Database schema state
- No schema changes in this phase.
- Existing `User`, `Member`, `FamilyTree`, `Subscription`, `Plan` schema state remains unchanged.

Validation/build/runtime checks
- `cd server && npm run build` -> passed.
- `cd client && npm run build` -> passed.

Pending tasks
- Optional: add integration tests for `PUT /api/v1/member/remove-relation` across all relation types.
- Optional: add E2E tests for modal action tabs (`Edit`, `Remove Relationship`, `Delete`) and focus fallback after delete.
- Optional: localize new modal action labels/messages with i18n keys.

[2026-03-01 | Phase Update | RELATION INTELLIGENCE ENHANCEMENT]
- Scope followed: continued existing modules only, no regeneration.
- Existing data compatibility preserved by limiting strict gender-parent enforcement to new parent assignment actions.

Completed modules
1) Auto gender selection in Add Member modal (frontend)
- Added relation-aware auto gender suggestion when relation type changes:
  - `Father`, `Brother`, `Son` -> `male`
  - `Mother`, `Sister`, `Daughter` -> `female`
  - `Spouse` -> opposite of focused member gender (if focused member is male/female)
  - focused member gender unknown -> `unspecified` (blank-equivalent)
- User can still manually change gender before submit.

2) Spouse opposite-gender backend safety (non-forcing)
- Added backend safety default during spouse creation:
  - if incoming spouse member has no explicit gender value, and related focused member has male/female gender,
  - spouse gender defaults to opposite.
- Explicit user-provided gender is preserved (no forced override).

3) Child auto-parent linking intelligence
- Enhanced child-add relation handling:
  - focused parent gender `male` -> child `fatherId = focused member`
  - focused parent gender `female` -> child `motherId = focused member`
  - focused parent with spouse -> system auto-assigns the other parent from preferred spouse when valid.
- Spouse selection prefers opposite binary gender match when available; otherwise falls back deterministically.

4) Relation consistency improvements
- Child-parent linkage now auto-includes spouse parent assignment where valid, so child appears under both parents through derived rebuild.
- Existing dedupe + reciprocal rebuild logic remains in place (`children`, `siblings`, `spouses`) to avoid duplicates and inconsistencies.

5) Backend validation enhancements
- Added strict parent-role gender validation for new parent assignments:
  - `fatherId` cannot reference female member
  - `motherId` cannot reference male member
  - father/mother cannot share same known binary gender when both specified
- Existing protections retained:
  - self-parent prevention
  - same-parent-id prevention
  - ancestry cycle prevention
- Added optional soft warnings for age inconsistency:
  - if parent birth date is after child birth date, response includes non-blocking warning text.

6) Frontend child add behavior
- After adding a child via focused parent:
  - focus now remains on parent (no jump to new child)
  - focus bundle refreshes immediately so new child appears in child list.

Data compatibility safety
- To avoid breaking legacy/inconsistent historical records:
  - strict parent gender constraints are enforced on new/changed parent assignments and connect operations.
  - non-parent-field member updates remain compatible.

File changes
- Server
  - `server/src/controllers/memberController.js`
- Client
  - `client/app/tree/[id]/page.tsx`
- Docs/Progress
  - `ProjectTillNow.txt`

Database schema state
- No schema changes in this phase.
- Existing `User`, `Member`, `FamilyTree`, `Plan`, `Subscription` schemas unchanged.

Validation/build/runtime checks
- `cd server && npm run build` -> passed.
- `cd client && npm run build` -> passed.

Pending tasks
- Optional: add integration tests for child auto-parent linking when parent has multiple spouses.
- Optional: expose soft age warnings in dedicated frontend warning banner near add/update member flow.
- Optional: extend spouse auto-parent pick strategy with explicit active-spouse priority metadata if product rules require it.

[2026-03-01 | Phase Addendum | Relation Intelligence UX Exactness]
- Follow-up refinement applied to match exact UX requirement for unknown spouse gender.

Completed refinements
1) True blank gender behavior in Add Member modal
- Changed add-member form gender state to support empty value (`""`) as true blank.
- Added `Select gender` option in gender dropdown.
- For `relationType = spouse` and focused member gender unknown:
  - gender now stays blank (not auto-set to `unspecified`).
- User can still manually select `male` / `female` / `other` / `unspecified` as override.

2) Payload compatibility for spouse default safety
- Add-member payload now sends `gender` only when explicitly selected.
- When blank, `gender` is omitted so backend spouse default-opposite logic can safely apply as fallback.

File changes
- `client/app/tree/[id]/page.tsx`
- `ProjectTillNow.txt`

Validation/build checks
- `cd client && npm run build` -> passed.
- `cd server && npm run build` -> passed.

Data safety
- No schema changes.
- Existing data unaffected.

[2026-03-01 | Phase Update | AUTO SPOUSE LINK FOR CHILD PARENTS]
- Scope followed: continued existing modules only, no regeneration.
- Existing data compatibility preserved (no schema migration and no destructive cleanup).

Completed modules
1) Auto spouse-linking for child parents
- Implemented backend integrity helper to auto-link parents as spouses when a child has both `fatherId` and `motherId`:
  - father includes mother in `spouses`
  - mother includes father in `spouses`
- Uses `$addToSet` so duplicates are prevented automatically.

2) Applied during child creation and update
- Child creation path now invokes parent spouse-linking once parent assignment is validated.
- Member update path now invokes parent spouse-linking when parent ids are present/changed.

3) Transaction safety enhancement
- `updateMember` flow moved into `withMongoTransaction` so parent validation + spouse-linking + member save + derived rebuild execute atomically.
- Create flow already used transaction and remains transactional.

4) Extended integrity for parent mutation flows
- Relation mutation (`father`, `mother`, `child`) now also enforces parent spouse-linking after parent assignment validation, keeping behavior consistent across relation engine operations.

5) Edge/integrity handling
- Prevents invalid/self spouse linking by guarding identical parent ids.
- Does not remove spouse links automatically when one parent is later deleted (as requested; optional skipped).
- Polygamy support preserved: existing spouse arrays are not overwritten; only missing counterpart is appended.

File changes
- Server
  - `server/src/controllers/memberController.js`
- Docs/Progress
  - `ProjectTillNow.txt`

Database schema state
- No schema/index changes in this phase.
- Existing trees/data structure unchanged.

Validation/build checks
- `cd server && npm run build` -> passed.
- `cd client && npm run build` -> passed.

Pending tasks
- Optional: add regression integration tests for parent auto-linking across:
  - create child with two parents,
  - update child parent ids,
  - relation engine connect flows (`father`/`mother`/`child`).
- Optional: add lightweight admin repair endpoint/script to backfill spouse links for historical child-parent pairs without requiring edits.

[2026-03-01 | Overall Working Check | End-to-End Validation]
- Scope followed: validation-only pass on current implementation state (no module regeneration).
- Request covered: overall project working check + progress log append.

Completed validation modules
1) Project state review
- Reviewed latest `ProjectTillNow.txt` and confirmed recent phases are recorded up to:
  - relation intelligence enhancement,
  - child-parent spouse auto-linking.

2) Server integrity validation
- Ran `cd server && npm run integrity:check`.
- Result summary:
  - `treesScanned: 8`
  - `membersScanned: 32`
  - `brokenReferences: 0`
  - `circularSpouseLoops: 0`
  - `duplicateRelationshipEntries: 0`
- Confirms no duplicate relation entries and no structural reference breaks in current DB snapshot.

3) Server runtime health checks
- Live API check passed:
  - `GET /api/v1/health` -> `200` with `dbConnected: true`.
- Auth-protected route behavior sanity:
  - `GET /api/v1/subscription/plans` (without token) -> `401` (expected access control behavior).

4) Client build and runtime checks
- Client build passed: `cd client && npm run build`.
- Production runtime smoke (fresh build, isolated port):
  - started `next start` on `:3020`
  - `GET /` -> `200`
  - `GET /login` -> `200`
  - `GET /dashboard` -> `200`
- Note:
  - If `.next` is in dev-state (missing `BUILD_ID`), `next start` may fail until `next build` is run again.

5) Build checks (current code)
- `cd server && npm run build` -> passed.
- `cd client && npm run build` -> passed.

File changes
- `ProjectTillNow.txt`

Database schema state
- No schema/index changes in this validation step.
- Existing model structure remains unchanged.

Pending tasks
- Optional: add a single command workflow for local validation (`integrity:check` + server health + client build smoke) to reduce manual steps.
- Optional: add automated E2E smoke tests for `/login`, `/dashboard`, and key tree flows.

[2026-03-01 | Phase Update | REVERSE RELATION INTELLIGENCE FIX]
- Scope followed: continued existing modules only, no regeneration.
- Existing data compatibility preserved by additive relationship backfill behavior and non-destructive validation handling.

Completed modules
1) Reverse parent linking on spouse connect (backend)
- Added reverse-link helper to backfill missing second parent when spouse relation is connected:
  - If child has `fatherId = A` and `motherId` missing -> set `motherId = B`
  - If child has `motherId = A` and `fatherId` missing -> set `fatherId = B`
- Added reciprocal child association update:
  - linked spouse gets affected children via `$addToSet` (`$each`) to avoid duplicates.
- Safety constraints retained:
  - no self-spouse linking,
  - no duplicate relation entries,
  - parent assignment validated before update.
- Validation failures for incompatible legacy edge cases are skipped per-child (non-destructive), so spouse link can still be saved without corrupting existing trees.

2) Spouse-child auto linking coverage points
- Applied reverse backfill in both spouse-link paths:
  - New-member spouse attach flow (`attachRelationToNewMember`)
  - Existing-member relation engine spouse connect (`mutateMemberRelation`)
- Existing `rebuildDerivedRelations` pipeline remains active, keeping graph consistency across `children/spouses/siblings`.

3) Transaction integrity enhancement
- `PATCH /api/v1/trees/:treeId/members/:memberId/relations` (`updateMemberRelation`) is now wrapped in `withMongoTransaction`.
- Tree lookup, source/target load, relation mutation, and response rebuild execute atomically with shared session.

4) Member details 400 validation issue fix
- Confirmed relation endpoint path remains:
  - `GET /api/v1/trees/:treeId/members/:memberId/relations`
- Controller hardening:
  - normalizes/parses `treeId` and `memberId`
  - invalid/empty `memberId` returns `404 Member not found` (not 400).
- Actual 400 root cause resolved on frontend:
  - detail modal was requesting `childrenLimit: 120`, while backend max is 100.
  - updated request to `childrenLimit: 100`.

5) Frontend 404 safety behavior
- Member detail modal error handling now maps 404-style failures to:
  - `Member not found`
- Prevents showing generic `Validation failed` for missing/invalid member scenarios.

File changes
- Server
  - `server/src/controllers/memberController.js`
- Client
  - `client/app/tree/[id]/page.tsx`
- Docs/Progress
  - `ProjectTillNow.txt`

Database schema state
- No schema/index changes in this phase.
- Existing collections and historical tree data remain intact.

Validation/build checks
- `cd server && npm run build` -> passed.
- `cd client && npm run build` -> passed.

Functional verification notes
- Relation flow supported:
  - add child (single parent) -> add parent spouse -> child receives missing second parent when valid.
- Member details endpoint now avoids prior 400 caused by oversized query limit in UI.

Pending tasks
- Optional: add integration test for reverse spouse-parent backfill flow:
  - child with one parent -> connect spouse -> child second parent auto-filled.
- Optional: add dedicated API regression test asserting invalid `memberId` on relations endpoint returns 404.
- Optional: add telemetry/debug log for skipped child backfills (validation-skipped edge cases) to aid admin audits.

[2026-03-01 | Phase Update | CLEAN HEADER ACTIONS]
- Scope followed: existing modules only, no regeneration.
- Request covered: tree header action cleanup while preserving core share/export flow.

Completed modules
1) Header action cleanup on Tree page
- Removed action button: `Export JSON`.
- Removed social buttons: `Twitter` and `Facebook`.
- Kept action buttons:
  - `Export PDF`
  - `Copy Link`
  - `WhatsApp` (retained as optional existing share channel)

2) Dead logic removal (no unused action code left)
- Removed `exportTreeAsJson` callback implementation.
- Simplified export state type from `"json" | "pdf" | null` to `"pdf" | null`.
- Replaced multi-platform share helper with WhatsApp-only helper:
  - removed `openSocialShare(...)`
  - added `openWhatsAppShare()`.
- Ensured no broken imports or leftover references from removed buttons/functions.

3) Layout alignment
- Header action container remains `flex` with existing spacing classes.
- Action row alignment and wrapping behavior preserved.

File changes
- Client
  - `client/app/tree/[id]/page.tsx`
- Docs/Progress
  - `ProjectTillNow.txt`

Database schema state
- No backend/schema/index changes in this phase.
- Existing data and relation model remain unchanged.

Validation/build checks
- `cd client && npm run build` -> passed.
- `cd server && npm run build` -> passed.

Pending tasks
- Optional: remove now-unused i18n keys (`tree.exportJson`, `tree.exportingJson`, `tree.shareTwitter`, `tree.shareFacebook`) from locale files for string hygiene.

[2026-03-01 | Phase Update | HEADER ACTION CLEANUP]
- Scope followed: existing modules only, no regeneration.
- Request covered: final tree header action cleanup with PDF export safety.

Completed modules
1) Tree header actions simplified (frontend only)
- Removed from Tree page header UI:
  - `Export JSON`
  - `Twitter` share
  - `Facebook` share
  - `WhatsApp` share
- Kept header actions exactly as requested:
  - `Export PDF`
  - `Copy Link`
  - `Add Member`
  - `Back to Dashboard`

2) Dead social/share cleanup
- Removed WhatsApp-only social helper function from tree page.
- No social-share utility/button path remains in tree header component logic.

3) JSON export safety constraint respected
- Frontend access for JSON export remains removed.
- Backend/export capability was not deleted or modified (kept for future use).

4) Alignment and code hygiene
- Header layout remains aligned using existing flex/wrap classes.
- No unused imports introduced.
- No unused API call introduced.
- No console build/type errors from this cleanup.

File changes
- Client
  - `client/app/tree/[id]/page.tsx`
- Docs/Progress
  - `ProjectTillNow.txt`

Database schema state
- No backend/schema/index changes in this phase.
- Existing data model unchanged.

Validation/build checks
- `cd client && npm run build` -> passed.
- `cd server && npm run build` -> passed.

Pending tasks
- Optional: remove now-unused i18n key `tree.shareWhatsapp` from locale files for string hygiene.

[2026-03-01 | Phase Update | PROFILE PHOTO DISPLAY IN TREE]
- Scope followed: existing modules only, no regeneration.
- Request covered: tree node profile photo rendering enhancement in `FocusTreeCanvas` while preserving existing tree logic and interaction behavior.

Completed modules
1) Profile photo rendering enhancement (tree nodes)
- Kept existing D3/SVG node pipeline intact and enhanced avatar fallback resilience.
- For each node:
  - If `member.profileImage` exists and URL is valid, render clipped circular avatar image.
  - Avatar sizing remains within requested range:
    - focus node: `58px`
    - other nodes: `50px`
- Circular clipping and cover behavior retained:
  - circle clip path for strict round shape
  - `preserveAspectRatio="xMidYMid slice"` for cover-style cropping without distortion
- Soft border retained with light gray ring (`#cbd5e1`) and existing clean node shadow.

2) Default avatar logic (no image / failed image)
- If no profile image URL:
  - render fallback circular avatar background by gender:
    - male -> light blue (`#dbeafe`)
    - female -> light pink (`#fce7f3`)
    - unknown/other -> gray (`#e2e8f0`)
  - show first letter of member name in uppercase.
- If image fails to load:
  - auto-switch to fallback avatar immediately on error event.
  - failed URL is cached in a memoized `Set` state to prevent repeated failing image attempts.

3) Performance and safety
- No render-loop behavior introduced:
  - failed URLs are deduplicated (`Set`) so each broken URL triggers at most one state transition.
- Avatar config remains memoized (`useMemo`) and avatar drawing callback remains memoized (`useCallback`).
- Tree navigation/zoom/focus logic unchanged.
- Note on Next.js Image:
  - `FocusTreeCanvas` renders inside an SVG/D3 pipeline; Next.js `Image` is not directly compatible inside SVG node drawing, so native SVG `<image>` is used for this path.

File changes
- Client
  - `client/components/FocusTreeCanvas.tsx`
- Docs/Progress
  - `ProjectTillNow.txt`

Database schema state
- No backend/schema/index changes in this phase.
- Existing collections and relations model unchanged.

Validation/build checks
- `cd client && npm run build` -> passed (clean, no lint/type warnings).

Pending tasks
- Optional: add a small UI E2E check that simulates image load failure and asserts fallback initial/gender-color rendering in tree nodes.

[2026-03-01 | Phase Update | PLAN CONFIGURATION UPDATE]
- Scope followed: existing modules only, no regeneration.
- Request covered: seeded/updated subscription plans and pricing UI updates while keeping Razorpay integration logic intact.

Completed modules
1) Plan collection configuration sync (idempotent)
- Added core plan definitions (4 plans):
  - Free (default): monthly 0, yearly 0, maxMembers 10, maxTrees 1, features ["Basic Tree"], active
  - Basic: monthly 99, yearly 999, maxMembers 100, maxTrees 1, features ["Extended Members"], active
  - Pro: monthly 199, yearly 1999, maxMembers 500, maxTrees 1, features ["Large Family Support"], active
  - Family Plus: monthly 299, yearly 2999, maxMembers 999999, maxTrees 1, features ["Unlimited Members"], active
- Implemented idempotent upsert flow by plan name (case-insensitive match), so reruns update existing records without duplicating plan entries.
- Enforced single default plan:
  - Free plan is always set as default.
  - Any other default flags are cleared.

2) Plan sync execution path
- Server startup now ensures configured plans after DB connect.
- Public plan listing also ensures configured plans before returning pricing plans.
- Public pricing plan output is constrained to configured core plan names to keep pricing catalog aligned.

3) Pricing page UI update (no Razorpay flow change)
- Billing toggle retained and verified (Monthly / Yearly).
- Price formatting updated to explicit INR symbol (`\u20B9` => rupee symbol display).
- Pro plan highlighted as `Most Popular` with visual badge and card emphasis.
- Existing subscribe/checkout/verify logic and Razorpay APIs were not modified.

File changes
- Server
  - `server/src/utils/subscriptionService.js`
  - `server/src/server.js`
- Client
  - `client/app/pricing/page.tsx`
- Docs/Progress
  - `ProjectTillNow.txt`

Database schema state
- No schema/index changes in this phase.
- Existing plan/subscription collections retained; only plan documents are seeded/updated in-place.

Validation/build checks
- `cd server && npm run build` -> passed.
- `cd client && npm run build` -> passed.
- Pricing page route compiled successfully in production build (`/pricing`), with updated UI logic included.

Pending tasks
- Optional: run a one-time DB verification query in target environment to confirm exactly one `isDefault: true` plan document and that it is `Free`.
- Optional: if legacy non-core active plans should be retired, perform controlled migration/deactivation after confirming no active subscriptions depend on them.

Verification notes
- Live DB sync executed: `ensureConfiguredPlans()`.
- Configured plan keys resolved: `free, basic, pro, family plus`.
- Default plan check: exactly one default, and it is `Free`.

[2026-03-02 | Phase Update | EMAIL DEBUG ENDPOINT (SMTP/RESEND)]
- Scope followed: debug-only email workflow updates; no feature/module regeneration.
- Request covered: temporary SMTP/Resend debug endpoint, env validation, and full SMTP response/error logging.

Completed modules
1) Debug test-email API endpoint
- Added unauthenticated `GET /api/v1/test-email` endpoint.
- Sends a plain-text test email to `debug+test@your-email.com` with current ISO timestamp.
- Does not require user creation/login.

2) SMTP env validation in test route
- Validates the required env variables before sending:
  - `SMTP_HOST`
  - `SMTP_PORT`
  - `SMTP_USER`
  - `SMTP_PASS`
  - `EMAIL_FROM`
- Returns structured failure JSON when any value is missing.

3) Full SMTP response/error logging
- Extended shared email service to return SMTP response metadata.
- Added logger-based success and error logs with SMTP response payload details.
- Added force-send path for debug flow so endpoint can test SMTP without account creation flow.

4) API docs update
- Appended debug endpoint details in `docs/API.md` with request purpose, env checks, and response schema.

File changes
- Server
  - `server/src/utils/emailService.js`
  - `server/src/controllers/testEmailController.js`
  - `server/src/routes/testEmailRoutes.js`
  - `server/src/routes/apiV1/index.js`
- Docs/Progress
  - `docs/API.md`
  - `ProjectTillNow.txt`

Database schema state
- No schema/index/model changes in this phase.
- Existing Mongo collections and relationships unchanged.

Validation/build checks
- `cd server && npm run build` -> passed (`check-syntax` success).

Pending tasks
- Temporary debug route should be removed or access-restricted after SMTP troubleshooting is complete.
- Optional: replace fixed debug recipient with env-configurable value once debugging phase ends.
